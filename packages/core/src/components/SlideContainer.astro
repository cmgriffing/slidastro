---
interface Props {
  width: number;
  height: number;
  id?: string;
}

const { width, height, id = 'slide-content-wrapper' } = Astro.props;
---

<div class="slide-container" style={`width: ${width}px; height: ${height}px;`}>
  <div class="slide-content" id={id}>
    <slot />
  </div>
  <svg id="drauu-layer" class="drauu-layer"></svg>
</div>

<style>
  .slide-container {
    position: relative;
    overflow: hidden;
    background: white;
    box-shadow: 0 10px 25px rgba(0,0,0,0.1);
  }
  .slide-content {
    width: 100%;
    height: 100%;
    transform-origin: top left;
  }
  .drauu-layer {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 100;
  }
</style>
