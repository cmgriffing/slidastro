---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - packages/cli/package.json
  - packages/cli/src/index.ts
  - packages/cli/bin/slidastro.mjs
  - packages/core/package.json
  - packages/core/src/index.ts
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "User can run `slidastro dev slides.md` and see the CLI respond"
    - "The CLI correctly stubs `build` and `export` commands"
    - "The Astro programmatic API is invoked with the slidastro integration"
  artifacts:
    - path: "packages/cli/bin/slidastro.mjs"
      provides: "CLI executable entry point"
    - path: "packages/core/src/index.ts"
      provides: "Astro integration plugin"
  key_links:
    - from: "slidastro"
      to: "astro"
      via: "programmatic dev() call"
---

<objective>
Implement the Slidastro CLI using `cac` and establish the core Astro integration that orchestrates the slide presentation environment.

Purpose: Main entry point and orchestration layer.
Output: Working CLI that can start an Astro dev server.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create @slidastro/cli with cac</name>
  <files>packages/cli/package.json, packages/cli/src/index.ts, packages/cli/bin/slidastro.mjs</files>
  <action>
    - Initialize `packages/cli/package.json` with `cac`, `astro`, and workspace dependencies (`@slidastro/core`, `@slidastro/parser`).
    - Implement CLI commands in `packages/cli/src/index.ts` using `cac`:
      - `dev <entry>`: The primary focus.
      - `build <entry>`: Stub returning "Not implemented".
      - `export <entry>`: Stub returning "Not implemented".
    - Create `packages/cli/bin/slidastro.mjs` as the executable script (chmod +x).
  </action>
  <verify>
    <automated>pnpm -F @slidastro/cli exec node bin/slidastro.mjs --help</automated>
  </verify>
  <done>CLI commands defined and executable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Core Astro Integration Skeleton</name>
  <files>packages/core/package.json, packages/core/src/index.ts</files>
  <action>
    - Initialize `packages/core/package.json` with `astro`.
    - Implement `slidastroIntegration()` in `packages/core/src/index.ts`:
      - Accept options (entry file path).
      - Implement `astro:config:setup` hook.
      - Inject a catch-all route `/[...no]` pointing to a placeholder page (to be created in Plan 04).
  </action>
  <verify>
    <automated>ls packages/core/src/index.ts</automated>
  </verify>
  <done>Astro integration skeleton exported.</done>
</task>

<task type="auto">
  <name>Task 3: Wire CLI to Astro Programmatic API</name>
  <files>packages/cli/src/index.ts</files>
  <action>
    - In `packages/cli/src/index.ts`, implement the `dev` command:
      - Use `import { dev } from 'astro'` to start the server programmatically.
      - Pass `slidastroIntegration({ entry })` to the `integrations` array.
      - Handle server startup and logging.
  </action>
  <verify>
    <automated>ls packages/cli/src/index.ts</automated>
  </verify>
  <done>CLI dev command starts Astro server with integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Arguments | User-provided filepath is untrusted until validated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Information Disclosure | Local file path | mitigate | Ensure CLI only reads allowed markdown files |
| T-01-03-02 | Elevation of Privilege | Programmatic API | accept | Local dev tool execution is trusted |
</threat_model>

<verification>
Run `slidastro dev tests/slides.md` (creating a dummy file first) and ensure Astro server attempts to start.
</verification>

<success_criteria>
`slidastro dev` successfully invokes Astro's programmatic server with the Slidastro integration.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
