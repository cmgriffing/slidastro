---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - packages/types/package.json
  - packages/types/src/index.ts
  - .gitignore
autonomous: true
requirements: [FOUND-01]

must_haves:
  truths:
    - "Monorepo is correctly initialized with pnpm workspaces"
    - "Shared types are available for downstream packages"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Package isolation"
    - path: "packages/types/src/index.ts"
      provides: "Shared interfaces (SlideInfo, SlidastroConfig)"
  key_links:
    - from: "packages/types"
      to: "monorepo"
      via: "workspace:* dependency"
---

<objective>
Initialize the monorepo structure and establish shared types that define the Slidastro data model.

Purpose: Foundation for package isolation and type-safe development.
Output: pnpm workspace with @slidastro/types package.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Monorepo and @slidastro/types</name>
  <files>package.json, pnpm-workspace.yaml, .gitignore, packages/types/package.json, packages/types/src/index.ts</files>
  <action>
    - Create root `package.json` with basic metadata.
    - Create `pnpm-workspace.yaml` with packages directory.
    - Setup `.gitignore` (node_modules, dist, .astro).
    - Create `packages/types` directory and `package.json`.
    - Implement `packages/types/src/index.ts` with base interfaces from ARCHITECTURE.md:
      - `SlideInfo`: raw content, index, title, frontmatter, note, etc.
      - `SlidastroConfig`: theme, aspectRatio, etc.
      - `SlidastroData`: slides[], config, etc.
  </action>
  <verify>
    <automated>pnpm install && ls packages/types/src/index.ts</automated>
  </verify>
  <done>Monorepo setup and @slidastro/types exported.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Project | All code within monorepo is trusted internal logic |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01-01 | Tampering | Types | accept | Low risk in build-only package |
</threat_model>

<verification>
Ensure pnpm can resolve the workspace structure.
</verification>

<success_criteria>
`pnpm install` succeeds and `@slidastro/types` contains the required interfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
