---
phase: 01-foundation
plan: 02
type: tdd
wave: 2
depends_on: ["01-01"]
files_modified:
  - packages/parser/package.json
  - packages/parser/src/index.ts
  - packages/parser/src/core.ts
  - packages/parser/test/parser.test.ts
autonomous: true
requirements: [FOUND-02]

must_haves:
  truths:
    - "The parser correctly splits slides by '---' separators"
    - "The parser skips '---' inside code blocks"
    - "The parser extracts frontmatter and notes from each slide"
  artifacts:
    - path: "packages/parser/src/core.ts"
      provides: "Slidev-compatible parsing logic"
  key_links:
    - from: "packages/parser"
      to: "packages/types"
      via: "SlidastroData interface"
---

<objective>
Implement the Slidev-compatible markdown parser using TDD to ensure robust handling of slide separators and metadata extraction.

Purpose: Core data pipeline for Slidastro.
Output: Working parser with passing unit tests.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@_slidev/packages/parser/src/core.ts
</context>

<feature>
  <name>Slidastro Markdown Parser</name>
  <files>packages/parser/src/core.ts, packages/parser/test/parser.test.ts</files>
  <behavior>
    - Split a markdown file into multiple slides at `---` lines.
    - Ignore `---` when inside triple-backtick code blocks.
    - Extract frontmatter (YAML) from each slide.
    - Extract speaker notes from HTML comments at the end of each slide.
    - Case: Single slide deck (no separators).
    - Case: Multi-slide deck.
    - Case: Dash in code block.
  </behavior>
  <implementation>
    - Use `yaml` package for frontmatter parsing.
    - Implement the line-by-line scanning logic similar to Slidev parser's `parseSync` method (referencing `_slidev/packages/parser/src/core.ts`).
    - Expose `parse(markdown: string, filepath: string): SlidastroData` as the primary API.
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Parser Logic</name>
  <files>packages/parser/package.json, packages/parser/src/index.ts, packages/parser/src/core.ts, packages/parser/test/parser.test.ts</files>
  <behavior>
    - Input: `Slide 1\n---\nSlide 2` -> Output: 2 slides.
    - Input: `Slide 1\n\`\`\`\n---\n\`\`\`\nSlide 2` -> Output: 1 slide (content contains code block).
    - Input: `---title: Hello---\n# Slide` -> Output: Frontmatter title: 'Hello'.
  </behavior>
  <action>
    - Initialize `packages/parser/package.json` with dependencies (`yaml`, `vitest`).
    - Create a suite of tests in `packages/parser/test/parser.test.ts` covering the cases above.
    - Implement the core splitting and frontmatter logic in `packages/parser/src/core.ts` until tests pass.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/parser exec vitest run</automated>
  </verify>
  <done>Parser implementation passes all unit tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File Input | Markdown file content is treated as untrusted data until parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Denial of Service | Parser | mitigate | Ensure no infinite loops in regex or line scanner |
| T-01-02-02 | Information Disclosure | Frontmatter | mitigate | Use safe YAML parsing (yaml.parse) |
</threat_model>

<verification>
Ensure all parser tests pass under Vitest.
</verification>

<success_criteria>
Parser tests cover all core Slidev-compatible splitting requirements.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
