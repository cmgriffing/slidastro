---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on: ["01-03"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/virtual.ts
  - packages/core/src/templates/SlideView.astro
autonomous: false
requirements: [FOUND-04, FOUND-05]

must_haves:
  truths:
    - "User can see slide content in the browser at http://localhost:4321/1"
    - "Edits to slides.md trigger an HMR update in the browser without full reload"
    - "The parser correctly separates slides shown in the browser"
  artifacts:
    - path: "packages/core/src/templates/SlideView.astro"
      provides: "The visual slide renderer"
    - path: "packages/core/src/virtual.ts"
      provides: "Vite virtual module bridge"
  key_links:
    - from: "slides.md"
      to: "browser"
      via: "HMR invalidation"
---

<objective>
Implement the Astro-Vite-Virtual Module bridge that enables slide rendering and Hot Module Reload (HMR) for markdown edits.

Purpose: Provide the visual slide presentation experience.
Output: Working slide presentation with HMR.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Virtual Modules</name>
  <files>packages/core/src/virtual.ts, packages/core/src/index.ts</files>
  <action>
    - Create `packages/core/src/virtual.ts` that exports a Vite plugin:
      - Intercepts `virtual:slidastro/slides`.
      - Provides the parsed `SlidastroData` from the entry file.
    - Update `@slidastro/core/src/index.ts` to include this Vite plugin in `astro:config:setup`.
  </action>
  <verify>
    <automated>ls packages/core/src/virtual.ts</automated>
  </verify>
  <done>Virtual module for slides data is functional.</done>
</task>

<task type="auto">
  <name>Task 2: Create Minimal Slide Rendering Page</name>
  <files>packages/core/src/templates/SlideView.astro, packages/core/src/index.ts</files>
  <action>
    - Create `packages/core/src/templates/SlideView.astro`:
      - Import slide data from `virtual:slidastro/slides`.
      - Determine current slide number from route params.
      - Render raw HTML content of the current slide.
    - Update the `injectRoute` call in `@slidastro/core/src/index.ts` to use this `.astro` file.
  </action>
  <verify>
    <automated>ls packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>Basic slide rendering page implemented.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 3: Verify Slide Rendering and HMR</name>
  <what-built>Full Phase 1 foundation: CLI, Parser, Integration, and Rendering with HMR.</what-built>
  <how-to-verify>
    - Create `demo.md`:
      ```markdown
      # Slide 1
      ---
      # Slide 2
      ```
    - Run `slidastro dev demo.md`.
    - Visit `http://localhost:4321/1`.
    - Verify "Slide 1" is visible.
    - Change `demo.md`'s "Slide 1" to "Slide 1 Updated".
    - Verify browser updates automatically without manual refresh (HMR).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Vite Virtual Module | Module content is derived from file system |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04-01 | Tampering | Virtual Module | accept | Local dev environment only |
</threat_model>

<verification>
Manual verification of HMR and slide rendering.
</verification>

<success_criteria>
`slidastro dev` works end-to-end: starts server, parses slides, renders first slide, and handles HMR.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
