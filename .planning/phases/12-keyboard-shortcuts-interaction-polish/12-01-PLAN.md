---
phase: 12-keyboard-shortcuts-interaction-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "packages/core/src/components/MasterOverlay.astro",
  "packages/core/src/components/Navigation.client.ts",
  "packages/core/src/components/MasterDashboard.astro",
  "packages/client/src/state.ts"
]
autonomous: true
requirements: ["LAYOUT-04"]

must_haves:
  truths:
    - "Pressing 'D', 'T', 'N' toggles respective UI elements with smooth transitions"
    - "Navigation keys (Arrows, Space, etc.) work across all views"
    - "Pressing 'H' or '?' opens the Master Dashboard (Help Overlay)"
    - "The Master Dashboard shows available shortcuts and tools"
  artifacts:
    - path: "packages/core/src/components/MasterDashboard.astro"
      provides: "Help/Shortcuts UI"
    - path: "packages/core/src/components/MasterOverlay.astro"
      provides: "Consolidated shortcut engine"
  key_links:
    - from: "MasterOverlay.astro"
      to: "Navigation.client.ts"
      via: "Function imports (next, prev, goToSlide)"
---

<objective>
Refactor and expand the keyboard shortcut system and implement the "Master Dashboard" (Help Overlay) with refined interactions.

Purpose: provide a professional-level developer experience and accessibility for presentation tools.
Output: A robust shortcut engine and a clean, interactive help overlay.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/components/MasterOverlay.astro
@packages/core/src/components/Navigation.client.ts
@packages/client/src/state.ts

<interfaces>
From packages/client/src/state.ts:
```typescript
export interface UIState {
  layer: 'audience' | 'presenter' | 'dev'
  showDrawing: boolean
  showTheme: boolean
  showNavigation: boolean
  showHelp: boolean
}
export function toggleUI(key: keyof Omit<UIState, 'layer'>);
```

From packages/core/src/components/Navigation.client.ts:
```typescript
export function next();
export function prev();
export function goToSlide(no: number);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate Shortcut Engine & Export Nav Helpers</name>
  <files>packages/core/src/components/Navigation.client.ts, packages/core/src/components/MasterOverlay.astro</files>
  <action>
    1. Update `Navigation.client.ts` to export `next`, `prev`, and `goToSlide`.
    2. Remove the `keydown` listener from `Navigation.client.ts` (it will move to MasterOverlay).
    3. Update `MasterOverlay.astro` script to import these helpers.
    4. Expand the `keydown` listener in `MasterOverlay.astro` to include:
       - Navigation: `ArrowRight`, `ArrowDown`, `Space`, `PageDown` (next); `ArrowLeft`, `ArrowUp`, `Backspace`, `PageUp` (prev); `Home` (first); `End` (last).
       - Tools: `d` (drawing), `t` (theme), `n` (navigation visibility), `h` or `?` (help/master).
       - Misc: `f` (toggle fullscreen via `document.documentElement.requestFullscreen()` or `exitFullscreen()`).
    5. Ensure `e.preventDefault()` is used for navigation keys to prevent scrolling/clashing.
  </action>
  <verify>
    <automated>Check that Navigation.client.ts exports required functions and MasterOverlay handles keys.</automated>
  </verify>
  <done>Shortcuts consolidated in MasterOverlay, navigation works as before but through the new engine.</done>
</task>

<task type="auto">
  <name>Task 2: Master Dashboard (Help Overlay) Component</name>
  <files>packages/core/src/components/MasterDashboard.astro, packages/core/src/components/MasterOverlay.astro</files>
  <action>
    1. Create `packages/core/src/components/MasterDashboard.astro`.
    2. Implement a clean, centered overlay using `backdrop-filter: blur(10px)` and semi-transparent background.
    3. Display a list of "Keyboard Shortcuts" and "Available Tools" (using icons from the respective toolbars if possible, or simple text).
    4. Group shortcuts by category: "Navigation", "Tools", "General".
    5. Include `MasterDashboard` in `MasterOverlay.astro` inside a `ui-layer` controlled by `state.showHelp`.
  </action>
  <verify>
    <automated>Check for file existence and integration in MasterOverlay.</automated>
  </verify>
  <done>Help dashboard UI exists and is toggleable via 'H' or '?'.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Interaction Polish & Transitions</name>
  <files>packages/core/src/components/MasterOverlay.astro</files>
  <action>
    1. Update `.ui-layer` CSS in `MasterOverlay.astro` to provide unique transitions per tool:
       - Drawing: Slide up from bottom (`transform: translateY(20px)` to `0`).
       - Theme: Slide in from top-right (`transform: translate(20px, -20px)` to `0`).
       - Navigation: Simple fade (as is).
       - Help: Scale in from center (`transform: scale(0.9)` to `1`).
    2. Add `cubic-bezier(0.4, 0, 0.2, 1)` to all transitions for a professional feel.
    3. Ensure `pointer-events: none` is correctly handled so overlays don't block clicks when hidden.
  </action>
  <verify>
    <automated>CSS transitions updated in MasterOverlay.astro.</automated>
  </verify>
  <done>UI transitions are smooth, specialized for each tool, and non-disruptive.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client → Browser APIs | Fullscreen and Navigation APIs are controlled locally |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | S | MasterOverlay | accept | Shortcuts are client-side only, no server impact. |
| T-12-02 | D | MasterOverlay | mitigate | Ensure shortcuts don't fire when typing in inputs/textareas (handled in Task 1). |
</threat_model>

<verification>
1. Manual test: Navigate through slides using all mapped keys (Space, Arrows, PageUp/Down).
2. Manual test: Press 'D', 'T', 'N', 'H' and verify smooth transitions and UI visibility.
3. Manual test: Press 'F' to enter/exit fullscreen.
4. Manual test: Verify Help dashboard lists all correct shortcuts.
</verification>

<success_criteria>
- 10+ standard navigation and tool shortcuts mapped and working.
- Master Dashboard (Help) accessible and informative.
- Tool transitions are unique, smooth, and professional-looking.
</success_criteria>

<output>
After completion, create `.planning/phases/12-keyboard-shortcuts-interaction-polish/12-01-SUMMARY.md`
</output>
