---
phase: 11-ui-separation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "packages/client/src/state.ts",
  "packages/core/src/components/MasterOverlay.astro",
  "packages/core/src/templates/SlideView.astro",
  "packages/core/src/templates/PresenterView.astro",
  "packages/core/src/components/DrawingToolbar.astro",
  "packages/core/src/components/ThemeToggle.astro",
  "packages/core/src/components/NavigationOverlay.astro"
]
autonomous: true
requirements: [LAYOUT-02, LAYOUT-03, LAYOUT-05]

must_haves:
  truths:
    - "Navigating to a slide route (e.g., '/1') shows only the slide content, with no visible toolbars by default."
    - "A 'Master UI' controller manages the state and visibility of different UI layers (Audience, Presenter, Dev)."
    - "Presenter-specific elements like DrawingToolbar and ThemeToggle are conditionally rendered."
    - "Keyboard shortcuts for 'D' and 'T' still function to toggle tools but are managed via the Master UI state."
  artifacts:
    - path: "packages/client/src/state.ts"
      provides: "UI state ($ui) and layer management"
    - path: "packages/core/src/components/MasterOverlay.astro"
      provides: "Central UI controller component"
    - path: "packages/core/src/components/NavigationOverlay.astro"
      provides: "Refactored navigation and page number UI"
  key_links:
    - from: "packages/core/src/templates/SlideView.astro"
      to: "packages/core/src/components/MasterOverlay.astro"
      via: "Astro component inclusion"
    - from: "packages/core/src/components/MasterOverlay.astro"
      to: "packages/client/src/state.ts"
      via: "$ui store subscription"
---

<objective>
Implement a clean audience view by default and a Master UI controller to manage UI layers (Audience, Presenter, Dev) and tool visibility.
Purpose: Professionalize the audience experience and provide a centralized way to manage presentation tools.
Output: Decoupled UI components and a state-aware Master Overlay.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@packages/client/src/state.ts
@packages/core/src/templates/SlideView.astro
@packages/core/src/templates/PresenterView.astro
@packages/core/src/components/DrawingToolbar.astro
@packages/core/src/components/ThemeToggle.astro

<interfaces>
From packages/client/src/state.ts:
```typescript
export const $page = atom<number>(1)
export const $clicks = atom<number>(0)
export const $nav = computed([$page, $clicks, $clicksTotal], (page, click, totalClicks) => ({ ... }))
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Implement UI State Management</name>
  <files>packages/client/src/state.ts</files>
  <action>
    - Add `UIState` interface and `$ui` map store in `packages/client/src/state.ts`.
    - `UIState` should include: `layer` ('audience' | 'presenter' | 'dev'), and booleans for `showDrawing`, `showTheme`, `showNavigation`, `showHelp`.
    - Default state: `layer: 'audience'`, all tool booleans `false`.
    - Add helper functions `toggleUI(key: keyof UIState)` and `setLayer(layer: UIState['layer'])`.
    - Ensure `$ui` state is local to the window (do not sync toolbar visibility across windows in `sync.ts` yet, as audience shouldn't see presenter's toolbar).
  </action>
  <verify>
    <automated>node -e "const { \$ui, toggleUI } = require('./packages/client/dist/index.mjs'); toggleUI('showDrawing'); if (!\$ui.get().showDrawing) throw new Error('Toggle failed');"</automated>
  </verify>
  <done>UI state is defined and toggleable via NanoStores.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create MasterOverlay and NavigationOverlay</name>
  <files>packages/core/src/components/MasterOverlay.astro, packages/core/src/components/NavigationOverlay.astro</files>
  <action>
    - Create `packages/core/src/components/NavigationOverlay.astro` by extracting the page number display logic from `SlideView.astro`. It should be hidden by default in 'audience' mode unless `showNavigation` is true.
    - Create `packages/core/src/components/MasterOverlay.astro`.
    - This component should include `DrawingToolbar.astro`, `ThemeToggle.astro`, and `NavigationOverlay.astro`.
    - Use a client-side script in `MasterOverlay.astro` to:
      1. Detect the current layer from the URL (path starts with `/presenter` -> 'presenter', else 'audience').
      2. Set initial `$ui` layer.
      3. Subscribe to `$ui` and toggle visibility (via CSS classes or `display: none`) of the child toolbars.
      4. Centralize keyboard shortcuts ('D' for drawing, 'T' for theme) to toggle `$ui` state instead of direct component interaction.
  </action>
  <verify>
    - `packages/core/src/components/MasterOverlay.astro` exists and imports toolbars.
    - Keyboard listeners for 'D' and 'T' are present in the script.
  </verify>
  <done>MasterOverlay component created and manages visibility of toolbars.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor Views for UI Separation</name>
  <files>packages/core/src/templates/SlideView.astro, packages/core/src/templates/PresenterView.astro, packages/core/src/components/DrawingToolbar.astro, packages/core/src/components/ThemeToggle.astro</files>
  <action>
    - In `SlideView.astro`:
      - Remove `DrawingToolbar` and `ThemeToggle` direct inclusions.
      - Remove the `page-number` div.
      - Include `<MasterOverlay />` at the end of `<body>`.
    - In `PresenterView.astro`:
      - Include `<MasterOverlay />` to ensure tools are available in presenter mode (but might be styled differently or always visible).
    - Update `DrawingToolbar.astro` and `ThemeToggle.astro` to be wrapped in a container that can be easily hidden/shown by `MasterOverlay` (e.g., adding a specific ID or class).
    - Ensure `DrawingToolbar` and `ThemeToggle` are hidden by default in CSS unless a class like `.ui-visible` is present on them or their container.
  </action>
  <verify>
    - Audience view (`/1`) shows no toolbars or page numbers initially.
    - Pressing 'T' shows the theme toggle.
    - Pressing 'D' shows the drawing toolbar.
  </verify>
  <done>Views are refactored to use the Master Controller, achieving a clean default state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Client -> UI State | Users can modify UI state via devtools/shortcuts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | UI State | accept | UI state is local and non-critical; tampering only affects local display. |
</threat_model>

<verification>
Run `pnpm dev` and visit `http://localhost:3000/1`. Verify no toolbars are visible. Press 'T' and 'D' to verify they appear. Visit `/presenter/1` and verify the UI behavior there.
</verification>

<success_criteria>
1. Audience view is completely clean (no toolbars, no page numbers) by default.
2. Master UI state correctly tracks visibility of layers and tools.
3. Toolbars can be toggled via keyboard shortcuts managed by the Master UI.
4. Presenter view still functions correctly and can access tools.
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-separation/11-01-SUMMARY.md`
</output>
