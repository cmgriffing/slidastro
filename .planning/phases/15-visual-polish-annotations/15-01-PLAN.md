---
phase: 15-visual-polish-annotations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/core/package.json, packages/core/src/components/SAutoFitText.astro, packages/core/src/components/SAutoFitText.client.ts, packages/core/src/renderer.ts]
autonomous: true
requirements: [VIS-02]

must_haves:
  truths:
    - "Text wrapped in <AutoFitText> scales to fit the container width/height"
    - "Scaling works in standard Markdown files"
  artifacts:
    - path: "packages/core/src/components/SAutoFitText.astro"
      provides: "Astro component for auto-fitting text"
    - path: "packages/core/src/components/SAutoFitText.client.ts"
      provides: "Binary-search sizing logic"
  key_links:
    - from: "packages/core/src/renderer.ts"
      to: "SAutoFitText.astro"
      via: "regex transformation"
---

<objective>
Implement the <AutoFitText> component to automatically adjust font size based on container dimensions.

Purpose: Enable responsive text scaling in presentations without manual font-size adjustments.
Output: Working SAutoFitText component and renderer integration.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@packages/core/src/renderer.ts
@_slidev/packages/client/builtin/AutoFitText.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure and Skeleton</name>
  <files>packages/core/package.json, packages/core/src/components/SAutoFitText.astro</files>
  <action>
    - Add `@vueuse/core` to `packages/core/package.json` dependencies.
    - Create `packages/core/src/components/SAutoFitText.astro` as a wrapper component.
    - The component should accept `max` (default 100) and `min` (default 30) props.
    - It should render a div with class `slidastro-autofit` and data attributes for max/min.
    - Reference: Similar to `_slidev/packages/client/builtin/AutoFitText.vue` but adapted for Astro.
  </action>
  <verify>
    <automated>grep "@vueuse/core" packages/core/package.json && ls packages/core/src/components/SAutoFitText.astro</automated>
  </verify>
  <done>Dependencies added and component skeleton created.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Binary-Search Sizing Logic</name>
  <files>packages/core/src/components/SAutoFitText.client.ts</files>
  <action>
    - Create `packages/core/src/components/SAutoFitText.client.ts`.
    - Implement a function `initAutoFitText()` that finds all `.slidastro-autofit` elements.
    - For each element, use a binary-search algorithm to find the optimal font size that fits the container without overflow.
    - Use `ResizeObserver` to re-calculate on size changes.
    - Ensure it waits for `document.fonts.ready` to avoid measurement race conditions (per research findings).
    - The algorithm should iterate until the font size is stable (within 0.1px) or max iterations reached.
  </action>
  <verify>
    <automated>grep "binarySearch" packages/core/src/components/SAutoFitText.client.ts || grep "while" packages/core/src/components/SAutoFitText.client.ts</automated>
  </verify>
  <done>Client-side sizing logic implemented with binary search.</done>
</task>

<task type="auto">
  <name>Task 3: Renderer Integration</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    - Update `packages/core/src/renderer.ts` to handle `<AutoFitText>` tags in `renderSlide`.
    - Use regex to transform `<AutoFitText max="100" min="50">Content</AutoFitText>` into `<div class="slidastro-autofit" data-max="100" data-min="50">Content</div>`.
    - Ensure it also handles the `:max` and `:min` syntax (Vue-style) for compatibility with Slidev snippets.
  </action>
  <verify>
    <automated>grep "slidastro-autofit" packages/core/src/renderer.ts</automated>
  </verify>
  <done>Renderer supports AutoFitText in standard Markdown slides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown→HTML | Content from .md files is rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01-01 | XSS | SAutoFitText | mitigate | Content is handled by markdown-it which escapes by default; ensure regex doesn't bypass this. |
</threat_model>

<verification>
Check that text in an `<AutoFitText>` block scales when the window is resized.
</verification>

<success_criteria>
- Text automatically scales to fill container.
- No layout overflow for autofitted text.
- Works in .md files without explicit imports.
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish-annotations/15-01-SUMMARY.md`
</output>
