---
phase: 15-visual-polish-annotations
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified: [packages/core/package.json, packages/core/src/components/SMark.astro, packages/core/src/components/SMark.client.ts, packages/core/src/renderer.ts]
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "Text wrapped in <s-mark> is highlighted with a hand-drawn effect"
    - "Highlights can be triggered by clicks (sequential reveals)"
  artifacts:
    - path: "packages/core/src/components/SMark.astro"
      provides: "Astro component for annotations"
    - path: "packages/core/src/components/SMark.client.ts"
      provides: "Rough-notation integration and click sync"
  key_links:
    - from: "packages/core/src/renderer.ts"
      to: "SMark.astro"
      via: "regex transformation"
---

<objective>
Implement the <s-mark> component for hand-drawn style annotations using rough-notation.

Purpose: Provide dynamic, engaging visual annotations that can be triggered by slide clicks.
Output: SMark component and click-synchronized annotation logic.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/PROJECT.md
@packages/core/src/renderer.ts
@packages/client/src/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Infrastructure and SMark Component</name>
  <files>packages/core/package.json, packages/core/src/components/SMark.astro</files>
  <action>
    - Add `rough-notation` to `packages/core/package.json` dependencies.
    - Create `packages/core/src/components/SMark.astro`.
    - Support props: `type` (default 'highlight'), `color`, `strokeWidth`, `duration`, `at`, `click`.
    - The component should render a span with class `slidastro-mark` and data attributes for rough-notation configuration.
    - If `at` or `click` is provided, it should also have the `slidastro-click` class for visibility sync.
  </action>
  <verify>
    <automated>grep "rough-notation" packages/core/package.json && ls packages/core/src/components/SMark.astro</automated>
  </verify>
  <done>SMark component skeleton and dependencies added.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Rough-Notation Logic & Sync</name>
  <files>packages/core/src/components/SMark.client.ts</files>
  <action>
    - Create `packages/core/src/components/SMark.client.ts`.
    - Implement `initSMark()` that finds all `.slidastro-mark` elements.
    - Initialize `annotate` from `rough-notation` for each element.
    - Integrate with `$clicks` store from `@slidastro/client`.
    - Listen for click changes: if the current click matches the `at`/`click` requirement of a mark, call `.show()`. If the user goes back, call `.hide()`.
    - Handle cases where the annotation should be shown by default (no `at`/`click` prop).
  </action>
  <verify>
    <automated>grep "annotate" packages/core/src/components/SMark.client.ts && grep "\$clicks.listen" packages/core/src/components/SMark.client.ts</automated>
  </verify>
  <done>SMark client-side logic with click synchronization implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Renderer Integration for S-Mark</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    - Update `packages/core/src/renderer.ts` to support `<s-mark>` in `renderSlide`.
    - Handle both the tag form: `<s-mark type="underline">Text</s-mark>`.
    - And optionally support an attribute form if possible, but prioritize the tag form first.
    - Ensure attributes like `at`, `color`, and `type` are passed through to the generated HTML.
  </action>
  <verify>
    <automated>grep "slidastro-mark" packages/core/src/renderer.ts</automated>
  </verify>
  <done>Renderer supports s-mark in standard Markdown slides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown→HTML | Content from .md files is rendered as HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02-01 | XSS | SMark | mitigate | Content is handled by markdown-it; ensure regex doesn't bypass this. |
</threat_model>

<verification>
Check that annotations appear when reaching the assigned click step.
</verification>

<success_criteria>
- Hand-drawn annotations appear correctly.
- Annotations trigger on the correct click.
- Works in .md files.
</success_criteria>

<output>
After completion, create `.planning/phases/15-visual-polish-annotations/15-02-SUMMARY.md`
</output>
