---
phase: 08-layout-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "packages/core/src/layouts/layoutHelper.ts",
  "packages/core/src/styles/layouts-base.css",
  "packages/core/uno.config.ts",
  "packages/core/src/index.ts",
  "packages/core/src/templates/SlideView.astro",
  "packages/core/src/layouts/default.astro",
  "packages/core/src/layouts/cover.astro",
  "packages/core/src/layouts/two-cols.astro"
]
autonomous: true
requirements: [POL-06, POL-07]
must_haves:
  truths:
    - "Base slide styling matches Slidev's visual identity (typography, spacing, colors)"
    - "UnoCSS integration supports Slidev-style shortcuts (bg-main, text-main, etc.)"
    - "Existing layouts (default, cover, two-cols) use the new professional styling"
  artifacts:
    - path: "packages/core/src/layouts/layoutHelper.ts"
      provides: "Common logic for layout backgrounds and asset resolution"
    - path: "packages/core/src/styles/layouts-base.css"
      provides: "Base typography and layout styles ported from Slidev"
    - path: "packages/core/uno.config.ts"
      provides: "Project-wide UnoCSS configuration matching Slidev"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/uno.config.ts"
      via: "UnoCSS configuration import"
    - from: "packages/core/src/templates/SlideView.astro"
      to: "packages/core/src/styles/layouts-base.css"
      via: "CSS import"
---

<objective>
Establish the foundational styling and layout infrastructure for Slidev parity. This includes porting the base CSS styles, configuring UnoCSS for Slidev-compatible shortcuts, and refining existing layouts to use the new system.

Purpose: Ensure professional "Slidev-level" visual quality and robust slot handling.
Output: Refined styling foundation and updated core layouts.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-layout-parity/RESEARCH.md
@_slidev/packages/client/styles/layouts-base.css
@_slidev/packages/client/uno.config.ts
@_slidev/packages/client/layoutHelper.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Layout Infrastructure (Helper & CSS)</name>
  <files>packages/core/src/layouts/layoutHelper.ts, packages/core/src/styles/layouts-base.css</files>
  <action>
    - Create `packages/core/src/layouts/layoutHelper.ts` by porting the logic from `_slidev/packages/client/layoutHelper.ts`. Include `handleBackground` and `resolveAssetUrl`.
    - Create `packages/core/src/styles/layouts-base.css` by porting `_slidev/packages/client/styles/layouts-base.css`. Replace `@apply` with standard UnoCSS-compatible syntax or keep it if UnoCSS transformerDirectives is planned (Task 2). 
    - Note: Ensure the base styles are scoped to `.slidev-layout` class.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/layoutHelper.ts packages/core/src/styles/layouts-base.css</automated>
  </verify>
  <done>Layout helper and base CSS implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Configure UnoCSS and Update Integration</name>
  <files>packages/core/uno.config.ts, packages/core/src/index.ts, packages/core/src/templates/SlideView.astro</files>
  <action>
    - Create `packages/core/uno.config.ts` based on `_slidev/packages/client/uno.config.ts`. Include shortcuts like `bg-main`, `text-main`, `text-primary`, `abs-tl`, etc. 
    - Include `presetWind3`, `presetAttributify`, `presetTypography`, and `transformerDirectives` in the config.
    - Update `packages/core/src/index.ts` to use `packages/core/uno.config.ts` in the UnoCSS integration. Ensure `injectReset: true`.
    - Update `packages/core/src/templates/SlideView.astro` to import `packages/core/src/styles/layouts-base.css` and remove redundant hardcoded styles in the `<style>` block (like font-size/padding in `.content-wrapper` if they conflict with Slidev base styles).
  </action>
  <verify>
    <automated>grep -q "uno.config" packages/core/src/index.ts && grep -q "layouts-base.css" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>UnoCSS configured and integrated with base styles.</done>
</task>

<task type="auto">
  <name>Task 3: Refine Existing Layouts</name>
  <files>packages/core/src/layouts/default.astro, packages/core/src/layouts/cover.astro, packages/core/src/layouts/two-cols.astro</files>
  <action>
    - Update `default.astro`: Use `class="slidev-layout default"`.
    - Update `cover.astro`: Use `class="slidev-layout cover"`. Handle `frontmatter.background` using `layoutHelper`.
    - Update `two-cols.astro`: Use `class="slidev-layout two-columns w-full h-full grid grid-cols-2"`. Add support for `left` slot alongside the default slot in the left column (per POL-07).
    - Ensure all layouts correctly use the `frontmatter` prop for styling.
  </action>
  <verify>
    <automated>grep -q "slidev-layout" packages/core/src/layouts/default.astro packages/core/src/layouts/cover.astro packages/core/src/layouts/two-cols.astro</automated>
  </verify>
  <done>Existing layouts refined for visual parity and slot robustness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontmatter -> CSS | User-provided background URLs/colors in frontmatter are used in inline styles. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | `layoutHelper.ts` | mitigate | Sanitization of background URLs/colors to prevent CSS injection. |
</threat_model>

<verification>
Check visual rendering of a slide with `layout: cover` and one with `layout: two-cols` using `slidastro dev`.
</verification>

<success_criteria>
- Slide layouts have consistent typography and spacing matching Slidev.
- `two-cols` layout correctly renders content in `left` and `right` slots.
- Background images work in `cover` layout.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-parity/08-01-SUMMARY.md`
</output>
