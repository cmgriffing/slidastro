---
phase: 08-layout-parity
plan: 02
type: execute
wave: 1
depends_on: ["08-01"]
files_modified: [
  "packages/core/src/layouts/center.astro",
  "packages/core/src/layouts/fact.astro",
  "packages/core/src/layouts/quote.astro",
  "packages/core/src/layouts/statement.astro",
  "packages/core/src/layouts/section.astro",
  "packages/core/src/layouts/end.astro"
]
autonomous: true
requirements: [POL-05]
---

<objective>
Implement Slidev-compatible content-focused layouts to achieve visual parity.
This plan focuses on layouts that center or style text-heavy content: center, fact, quote, statement, section, and end.

Purpose: Provide a rich set of built-in layouts for common slide types.
Output: Six new Astro layout components in `packages/core/src/layouts/`.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-layout-parity/08-01-SUMMARY.md
@_slidev/packages/client/layouts/center.vue
@_slidev/packages/client/layouts/fact.vue
@_slidev/packages/client/layouts/quote.vue
@_slidev/packages/client/layouts/statement.vue
@_slidev/packages/client/layouts/section.vue
@_slidev/packages/client/layouts/end.vue
@packages/core/src/layouts/layoutHelper.ts
@packages/core/src/styles/layouts-base.css

<interfaces>
From packages/core/src/layouts/layoutHelper.ts:
```typescript
export function handleBackground(background?: string, dim = false, backgroundSize = 'cover'): Record<string, string | undefined>;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Port Center & Fact layouts</name>
  <files>packages/core/src/layouts/center.astro, packages/core/src/layouts/fact.astro</files>
  <action>
    Create `center.astro` and `fact.astro` based on Slidev's implementation.
    - `center.astro`: Uses `grid place-content-center h-full` and a `my-auto` wrapper for the default slot.
    - `fact.astro`: Uses `flex flex-col h-full justify-center text-center text-2xl` style (Fact usually has larger text).
    - Both should use `handleBackground` from `layoutHelper.ts` for consistency with other layouts.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/center.astro packages/core/src/layouts/fact.astro</automated>
  </verify>
  <done>Center and Fact layouts created and support backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: Port Quote & Statement layouts</name>
  <files>packages/core/src/layouts/quote.astro, packages/core/src/layouts/statement.astro</files>
  <action>
    Create `quote.astro` and `statement.astro`.
    - `quote.astro`: Centered layout, usually used for large quotes. Use `flex flex-col h-full justify-center`.
    - `statement.astro`: Similar to quote but for bold statements. Use `flex flex-col h-full justify-center text-3xl font-bold`.
    - Ensure they both support backgrounds via `handleBackground`.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/quote.astro packages/core/src/layouts/statement.astro</automated>
  </verify>
  <done>Quote and Statement layouts created.</done>
</task>

<task type="auto">
  <name>Task 3: Port Section & End layouts</name>
  <files>packages/core/src/layouts/section.astro, packages/core/src/layouts/end.astro</files>
  <action>
    Create `section.astro` and `end.astro`.
    - `section.astro`: A simple layout for starting a new section. Use `flex flex-col h-full justify-center`.
    - `end.astro`: The standard "Thank You" slide.
    - Implement `end.astro` with the specific styling from Slidev: `bg-black text-white text-xl tracking-widest text-center grid place-content-center h-full`.
    - Support a default slot that defaults to "END" if empty.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/section.astro packages/core/src/layouts/end.astro</automated>
  </verify>
  <done>Section and End layouts created with parity styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Frontmatter → Layout Props | Untrusted background URLs or CSS class names could be injected. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | Information Disclosure | `layoutHelper.ts` | mitigate | `handleBackground` already uses `resolveAssetUrl` to keep paths safe. |
| T-08-02-02 | Denial of Service | `end.astro` | accept | Large background images could slow down rendering, but this is a build-time/local-tool concern. |
</threat_model>

<verification>
Check that all six new layouts are present in `packages/core/src/layouts/` and export a valid Astro component structure.
</verification>

<success_criteria>
All content-focused layouts (center, fact, quote, statement, section, end) are implemented and match Slidev's basic structure and styling.
</success_criteria>

<output>
After completion, create `.planning/phases/08-layout-parity/08-02-SUMMARY.md`
</output>
