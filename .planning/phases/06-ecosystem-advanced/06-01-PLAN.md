---
phase: 06-ecosystem-advanced
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  packages/core/src/components/Mermaid.client.ts,
  packages/core/src/components/Monaco.client.ts,
  packages/core/src/renderer.ts,
  packages/core/src/virtual.ts,
  cypress/e2e/mermaid.cy.ts,
  cypress/e2e/monaco.cy.ts
]
autonomous: true
requirements: [ADV-01, ADV-04]

must_haves:
  truths:
    - "Mermaid diagrams re-render automatically when the page theme (dark/light) changes."
    - "Monaco editor provides IntelliSense for external libraries (ATA) in code blocks."
  artifacts:
    - path: "packages/core/src/components/Mermaid.client.ts"
      provides: "Dynamic mermaid rendering with theme sync"
    - path: "packages/core/src/components/Monaco.client.ts"
      provides: "Monaco editor with Shiki highlighting and ATA"
  key_links:
    - from: "packages/core/src/renderer.ts"
      to: "packages/core/src/components/Monaco.client.ts"
      via: ".monaco-container class"
---

<objective>
Stabilize advanced rendering features: Mermaid diagrams and Monaco code editor.
Ensure Mermaid diagrams respond to theme changes and Monaco editor has full type acquisition (ATA) for external libraries.
</objective>

<execution_context>
The project uses Astro and Vite. Monaco workers need special handling in Vite. Mermaid v11 is used for diagrams.
</execution_context>

<context>
@.planning/phases/06-ecosystem-advanced/RESEARCH.md
@packages/core/src/components/Mermaid.client.ts
@packages/core/src/components/Monaco.client.ts
@packages/core/src/renderer.ts
@packages/core/src/virtual.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Test Scaffolds</name>
  <files>cypress/e2e/mermaid.cy.ts, cypress/e2e/monaco.cy.ts</files>
  <action>
    Create Cypress test files for Mermaid and Monaco features.
    - `mermaid.cy.ts`: Test that `.mermaid` divs contain generated SVGs and that theme changes trigger re-renders.
    - `monaco.cy.ts`: Test that Monaco editor loads and eventually shows IntelliSense for external imports (e.g., nanostores).
  </action>
  <verify>
    <automated>ls cypress/e2e/mermaid.cy.ts cypress/e2e/monaco.cy.ts</automated>
  </verify>
  <done>Test scaffolds exist and are ready for implementation.</done>
</task>

<task type="auto">
  <name>Refactor Mermaid with Theme Sync</name>
  <files>packages/core/src/components/Mermaid.client.ts, packages/core/src/renderer.ts</files>
  <action>
    - Update `Mermaid.client.ts` to use `mermaid.initialize` correctly with the current document theme on boot.
    - Ensure the `MutationObserver` on `document.documentElement` correctly calls `mermaid.initialize` and `mermaid.run` on theme changes.
    - Update `renderer.ts` to ensure `mermaid` code blocks are wrapped in `<div class="mermaid">` (per D-ADV-01).
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core exec cypress run --spec cypress/e2e/mermaid.cy.ts</automated>
  </verify>
  <done>Mermaid diagrams render and update their theme dynamically.</done>
</task>

<task type="auto">
  <name>Enhance Monaco with ATA and Feature Detection</name>
  <files>packages/core/src/components/Monaco.client.ts, packages/core/src/virtual.ts</files>
  <action>
    - Update `Monaco.client.ts` to ensure `@typescript/ata` is robust.
    - Update `virtual.ts` to refine `features.monaco`. Instead of a boolean, scan code blocks for imports to populate `types` and `deps` (per RESEARCH.md).
    - Ensure `MonacoEnvironment.getWorkerUrl` uses correct paths for all workers (json, css, html, ts, editor).
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core exec cypress run --spec cypress/e2e/monaco.cy.ts</automated>
  </verify>
  <done>Monaco editor has type safety for imports and workers load correctly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| User Content → DOM | Mermaid and Monaco render user-provided code/diagrams. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01-01 | Tampering | Mermaid/Drauu SVG | mitigate | Use `dompurify` on generated SVG content if needed (handled by mermaid internally mostly). |
| T-06-01-02 | Elevation of Privilege | Monaco Editor | mitigate | Ensure Monaco doesn't execute user code in the main thread context without explicit user action. |
</threat_model>

<success_criteria>
- Mermaid diagrams adapt to dark/light mode switches.
- Monaco editor shows types for `import { atom } from 'nanostores'` after a few seconds.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ecosystem-advanced/06-01-SUMMARY.md`
</output>
