---
phase: 06-ecosystem-advanced
plan: 03
type: execute
wave: 2
depends_on: ["06-01-PLAN.md"]
files_modified: [
  "packages/core/src/virtual.ts",
  "packages/core/package.json",
  "packages/cli/src/index.ts",
  "packages/cli/src/build.ts",
  "tests/virtual-splitting.test.ts",
  "tests/mdx-rendering.test.ts"
]
autonomous: true
requirements: [FOUND-03]
must_haves:
  truths:
    - "slidastro dev presentation.astro renders multiple slides correctly"
    - "slidastro dev presentation.mdx renders multiple slides correctly"
    - "Variables in the first Astro logic block are available in all slides"
    - "MDX frontmatter is inherited by all virtual slides within an .mdx file"
  artifacts:
    - path: "packages/core/src/virtual.ts"
      provides: "Multi-slide splitting logic for Astro and MDX"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/core/src/virtual.ts"
      via: "File extension detection (.astro, .mdx)"
---

<objective>
Enable native Astro and MDX file support for slide authoring, allowing users to leverage the full Astro ecosystem within their presentations.

Purpose: Provide a first-class developer experience for users who prefer native component-based slide authoring.
Output: Updated virtual module with multi-slide splitting and CLI support for .astro and .mdx extensions.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ecosystem-advanced/RESEARCH.md

<interfaces>
From packages/core/src/virtual.ts:
```typescript
export function slidastroVitePlugin(entry: string): Plugin;
```

From packages/parser/src/index.ts:
```typescript
export function parse(markdown: string, filepath?: string): SlidastroMarkdown;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Test Scaffolds</name>
  <files>tests/virtual-splitting.test.ts, tests/mdx-rendering.test.ts</files>
  <action>
    Create test files for multi-slide splitting and MDX rendering.
    - `tests/virtual-splitting.test.ts`: Vitest test for splitting `.astro` and `.mdx` files and verifying frontmatter/logic block inheritance.
    - `tests/mdx-rendering.test.ts`: Vitest test for MDX integration within the Astro pipeline.
  </action>
  <verify>
    <automated>ls tests/virtual-splitting.test.ts tests/mdx-rendering.test.ts</automated>
  </verify>
  <done>Test scaffolds exist.</done>
</task>

<task type="auto">
  <name>Task 1: Multi-slide Splitting Logic</name>
  <files>packages/core/src/virtual.ts</files>
  <action>
    - Update `virtual.ts` to handle `.astro` and `.mdx` files by splitting their content using the `---` separator.
    - per RESEARCH.md (Pitfall 1): For `.astro` files, identify the first code fence (logic block) and prepend it to every individual virtual slide component.
    - For `.mdx` files, ensure frontmatter is extracted and inherited/prepended to all virtual slides as needed (per user request).
    - Implement a `splitSlides(content: string, isAstro: boolean)` helper that returns an array of slide contents.
    - Ensure the virtual module correctly generates `virtual:slidastro/slide/${i}.astro` for each part.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core vitest run tests/virtual-splitting.test.ts</automated>
  </verify>
  <done>Virtual module correctly splits .astro and .mdx files into multiple slides while preserving the shared logic block and frontmatter.</done>
</task>

<task type="auto">
  <name>Task 2: MDX Integration</name>
  <files>packages/core/package.json, packages/core/src/virtual.ts</files>
  <action>
    - Add `@astrojs/mdx` to `packages/core/package.json` dependencies.
    - Update the Astro configuration generated within `virtual.ts` (or the integration logic) to include the MDX integration when an `.mdx` file is used as an entry point.
    - Ensure that MDX slides are rendered using the standard MDX pipeline while still benefiting from Slidastro's layout system.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core vitest run tests/mdx-rendering.test.ts</automated>
  </verify>
  <done>MDX files are correctly parsed and rendered as multi-slide presentations.</done>
</task>

<task type="auto">
  <name>Task 3: CLI & Entry Resolution</name>
  <files>packages/cli/src/index.ts, packages/cli/src/build.ts</files>
  <action>
    - Update `packages/cli/src/index.ts` to allow `.astro` and `.mdx` extensions in the `dev` and `build` commands.
    - Update entry path resolution to correctly identify these files.
    - Ensure that `slidastro build` correctly processes these native formats into the static output.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/cli exec slidastro dev demo.astro --dry-run</automated>
  </verify>
  <done>CLI accepts .astro and .mdx files as valid presentation entry points.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System → Virtual Module | Source files (.astro, .mdx) are read and transformed into virtual components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Tampering | Shared Frontmatter | mitigate | Ensure prepended frontmatter is correctly sanitized and does not break subsequent slide syntax |
</threat_model>

<success_criteria>
- Native .astro files with multiple slides (separated by ---) render correctly.
- Variables defined in the first Astro logic block are accessible in all slides of that file.
- Native .mdx files render correctly with Astro components and multiple slides.
- CLI accepts .astro and .mdx files without error.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ecosystem-advanced/06-03-SUMMARY.md`
</output>
