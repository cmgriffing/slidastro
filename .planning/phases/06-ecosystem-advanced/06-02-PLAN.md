---
phase: 06-ecosystem-advanced
plan: 02
type: execute
wave: 2
depends_on: ["06-01-PLAN.md"]
files_modified: [
  "packages/client/src/state.ts",
  "packages/client/src/sync.ts",
  "packages/core/src/components/Drauu.client.ts",
  "packages/core/src/components/DrawingToolbar.astro",
  "packages/client/src/logic/recording.ts",
  "packages/core/src/components/RecordingToolbar.astro",
  "packages/core/src/templates/SlideView.astro",
  "tests/drauu-sync.test.ts",
  "cypress/e2e/drawing.cy.ts",
  "tests/recording-logic.test.ts"
]
autonomous: true
requirements: [ADV-02, ADV-03]
must_haves:
  truths:
    - "Drawing on one slide persists when navigating away and back"
    - "Drawings sync in real-time between presenter and audience windows"
    - "Presenter can record screen + camera and download a .webm file"
  artifacts:
    - path: "packages/core/src/components/Drauu.client.ts"
      provides: "Drauu initialization and state sync"
    - path: "packages/client/src/logic/recording.ts"
      provides: "MediaRecorder logic for screen + camera"
  key_links:
    - from: "packages/core/src/components/Drauu.client.ts"
      to: "packages/client/src/state.ts"
      via: "Nano Store subscription ($drawings)"
---

<objective>
Implement live interactivity features including drawing annotations and session recording.

Purpose: Provide presenters with tools to engage the audience and capture sessions without external software.
Output: Integrated Drauu drawing layer and MediaRecorder-based recording toolbar.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-ecosystem-advanced/RESEARCH.md

<interfaces>
From packages/client/src/state.ts:
```typescript
export const $slideIndex = atom(0);
export const $isPresenter = atom(false);
```

From packages/client/src/sync.ts:
```typescript
export function syncState(state: any): void;
export function onSync(callback: (state: any) => void): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create Test Scaffolds</name>
  <files>tests/drauu-sync.test.ts, cypress/e2e/drawing.cy.ts, tests/recording-logic.test.ts</files>
  <action>
    Create test files for drawing and recording features.
    - `tests/drauu-sync.test.ts`: Vitest unit test for Nano Store sync logic.
    - `cypress/e2e/drawing.cy.ts`: Cypress E2E test for multi-window drawing sync.
    - `tests/recording-logic.test.ts`: Vitest unit test for MediaRecorder state transitions and composition logic.
  </action>
  <verify>
    <automated>ls tests/drauu-sync.test.ts cypress/e2e/drawing.cy.ts tests/recording-logic.test.ts</automated>
  </verify>
  <done>Test scaffolds exist.</done>
</task>

<task type="auto">
  <name>Task 1: Drauu Integration & State Sync</name>
  <files>packages/client/src/state.ts, packages/client/src/sync.ts, packages/core/src/components/Drauu.client.ts</files>
  <action>
    - Add `$drawings` map atom to `packages/client/src/state.ts` using `nano-stores` (key: slideIndex, value: serialized SVG string).
    - Implement `Drauu.client.ts` to initialize `drauu` on a global SVG layer.
    - Wire `drauu.on('end')` to update `$drawings` store and trigger `syncState`.
    - Handle incoming sync events in `sync.ts` to update `$drawings` and call `drauu.load()`.
    - per RESEARCH.md: Use `BroadcastChannel` for same-origin tabs and existing WebSocket layer for dev/remote sync.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core vitest run tests/drauu-sync.test.ts</automated>
  </verify>
  <done>Drawing state is preserved in Nano Store and synchronized across windows.</done>
</task>

<task type="auto">
  <name>Task 2: Drawing UI & Toolbar</name>
  <files>packages/core/src/components/DrawingToolbar.astro, packages/core/src/templates/SlideView.astro</files>
  <action>
    - Create `DrawingToolbar.astro` with buttons for pen, eraser, color picker, and "Clear All".
    - per RESEARCH.md: Set `pointer-events: none` on the drawing SVG layer by default; toggle to `auto` when drawing mode is active.
    - Inject the toolbar and SVG layer into `SlideView.astro`.
    - Implement persistence to `localStorage` for drawings (per ADV-02).
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core exec cypress run --spec cypress/e2e/drawing.cy.ts</automated>
  </verify>
  <done>Drawing toolbar is visible, functional, and drawing layer does not block slide interactions when inactive.</done>
</task>

<task type="auto">
  <name>Task 3: In-Browser Recording</name>
  <files>packages/client/src/logic/recording.ts, packages/core/src/components/RecordingToolbar.astro</files>
  <action>
    - Implement `recording.ts` using `MediaRecorder` API to capture `getDisplayMedia` (screen) and `getUserMedia` (camera).
    - Use a hidden canvas to composite the camera feed (picture-in-picture) onto the screen capture if both are active.
    - Create `RecordingToolbar.astro` with "Start/Stop Recording" buttons and a status indicator.
    - Ensure stopping the recording triggers a download of a `.webm` file.
  </action>
  <verify>
    <automated>pnpm -F @slidastro/core vitest run tests/recording-logic.test.ts</automated>
  </verify>
  <done>Presenter can record the presentation with camera overlay and receive a video file upon completion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Sync Layer | Shared drawing data crosses window/network boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Tampering | Drawing Data | mitigate | Sanitize SVG input using `dompurify` before calling `drauu.load()` (per RESEARCH.md) |
| T-06-02-02 | Information Disclosure | MediaRecorder | mitigate | Ensure recording only starts after explicit user permission via browser dialog |
| T-06-02-03 | Spoofing | Sync Layer | mitigate | Although out of scope for local sync, ensure BroadcastChannel doesn't accept messages from other domains (inherent to browser). |
</threat_model>

<success_criteria>
- Drauu drawing layer correctly overlays slides without blocking clicks.
- Drawing sync works across multiple tabs/windows via Nano Stores.
- Recording produces a valid .webm file containing both screen and camera feeds.
</success_criteria>

<output>
After completion, create `.planning/phases/06-ecosystem-advanced/06-02-SUMMARY.md`
</output>
