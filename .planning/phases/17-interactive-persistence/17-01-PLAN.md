---
phase: 17
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/core/src/components/SDrag.astro, packages/core/src/components/SDrag.client.ts, packages/core/src/renderer.ts, packages/core/src/templates/SlideView.astro]
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "Elements wrapped in <s-drag> are visually draggable in the browser."
    - "Draggable elements preserve their position relative to the slide container."
  artifacts:
    - path: "packages/core/src/components/SDrag.astro"
      provides: "HTML structure for draggable elements"
    - path: "packages/core/src/components/SDrag.client.ts"
      provides: "Draggable interactivity using @vueuse/core"
  key_links:
    - from: "packages/core/src/renderer.ts"
      to: "packages/core/src/components/SDrag.astro"
      via: "regex replacement of <s-drag> tags"
---

<objective>
Implement the `<s-drag>` component and its client-side dragging interaction.

Purpose: Allow users to move elements within a slide by wrapping them in `<s-drag>`.
Output: Working draggable elements in the presentation.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/renderer.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SDrag component and client-side logic</name>
  <files>packages/core/src/components/SDrag.astro, packages/core/src/components/SDrag.client.ts</files>
  <action>
    - Create `SDrag.astro` to render a `div` with class `slidastro-drag` and absolute positioning.
    - It should accept `x` and `y` props and pass them as `data-x` and `data-y`.
    - Create `SDrag.client.ts` implementing `initSDrag()`.
    - Use `useDraggable` from `@vueuse/core` to make `.slidastro-drag` elements movable.
    - Ensure dragging updates the `left` and `top` style properties.
    - Store the current position in `localStorage` temporarily or just in memory for now.
  </action>
  <verify>
    <automated>ls packages/core/src/components/SDrag.astro packages/core/src/components/SDrag.client.ts</automated>
  </verify>
  <done>SDrag component and client-side dragging logic created.</done>
</task>

<task type="auto">
  <name>Task 2: Update renderer to support s-drag in Markdown</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    - Add a regex replacement in `renderSlide` to handle `<s-drag>` tags.
    - Extract `x` and `y` attributes (default to 0).
    - Maintain a `dragIndex` per slide to assign a unique `data-drag-id` to each `<s-drag>` on a slide.
    - Transform `<s-drag x="10" y="20">content</s-drag>` into `<div class="slidastro-drag" style="position: absolute; left: 10px; top: 20px;" data-drag-id="0" data-x="10" data-y="20">content</div>`.
  </action>
  <verify>
    <automated>grep -n "s-drag" packages/core/src/renderer.ts</automated>
  </verify>
  <done>Renderer supports s-drag with unique indexing.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize SDrag in SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    - Import `initSDrag` from `../components/SDrag.client`.
    - Call `initSDrag()` in the client-side script block (both on initial load and `astro:after-swap`).
  </action>
  <verify>
    <automated>grep "initSDrag" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>SDrag initialized on the client side.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → State | User drag interaction updates local DOM state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01-01 | Tampering | SDrag.client.ts | mitigate | Validate x/y coordinates to be within reasonable bounds if necessary. |
</threat_model>

<verification>
Manual verification: Add `<s-drag x="100" y="100">Drag me</s-drag>` to a slide and verify it can be moved in the browser.
</verification>

<success_criteria>
1. Elements wrapped in `<s-drag>` appear at specified coordinates.
2. Elements can be moved via mouse/touch interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/17-interactive-persistence/17-01-SUMMARY.md`
</output>
