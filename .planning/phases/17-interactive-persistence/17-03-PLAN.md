---
phase: 17
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified: [packages/core/src/components/SDrag.client.ts, cypress/e2e/persistence.cy.ts]
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "Moving an element in the browser updates the source Markdown file."
    - "Positions are preserved across page reloads."
  artifacts:
    - path: "cypress/e2e/persistence.cy.ts"
      provides: "E2E test for drag persistence"
  key_links:
    - from: "packages/core/src/components/SDrag.client.ts"
      to: "packages/core/src/index.ts"
      via: "import.meta.hot.send('slidastro:update-pos', ...)"
---

<objective>
Connect the client and server for full persistence and verify with E2E tests.

Purpose: Complete the feedback loop between UI interaction and file storage.
Output: Verified interactive persistence for draggable elements.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/components/SDrag.client.ts
@packages/core/src/index.ts
@.planning/phases/17-interactive-persistence/17-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Emit update-pos event from client</name>
  <files>packages/core/src/components/SDrag.client.ts</files>
  <action>
    - In `SDrag.client.ts`, add an `onEnd` callback to `useDraggable`.
    - When dragging ends, use `import.meta.hot.send('slidastro:update-pos', ...)` to send the new position.
    - Payload should include:
      - `filepath`: from `slidesData.markdownFiles` or similar (discover path)
      - `slideIndex`: from the closest `.content-wrapper` data-index
      - `dragId`: from `data-drag-id`
      - `x`, `y`: the final rounded coordinates
    - Debounce or throttle if necessary, though `onEnd` is sufficient for persistence.
    - Add a visual "saving" indicator if possible.
  </action>
  <verify>
    <automated>grep "import.meta.hot.send" packages/core/src/components/SDrag.client.ts</automated>
  </verify>
  <done>Client emits position updates to the server.</done>
</task>

<task type="auto">
  <name>Task 2: Create E2E test for persistence</name>
  <files>cypress/e2e/persistence.cy.ts, tests/fixtures/drag-test.md</files>
  <action>
    - Create a fixture Markdown file `tests/fixtures/drag-test.md` with an `<s-drag>` element.
    - Create a Cypress test `cypress/e2e/persistence.cy.ts`.
    - The test should:
      1. Load the presentation.
      2. Drag the `<s-drag>` element to a new position.
      3. Verify the element moved in the DOM.
      4. Wait for the server to update the file (brief delay).
      5. Read the fixture file content and verify the `x` and `y` attributes were updated.
      6. Reload the page and verify the element is at the new position.
  </action>
  <verify>
    <automated>pnpm cypress run --spec cypress/e2e/persistence.cy.ts</automated>
  </verify>
  <done>Interactive persistence is verified by automated E2E tests.</done>
</task>

<task type="auto">
  <name>Task 3: Add visual feedback and polish</name>
  <files>packages/core/src/components/SDrag.client.ts</files>
  <action>
    - Add `cursor: move` or `cursor: grab` to `.slidastro-drag` elements.
    - Implement a "ghost" or "active" style during dragging (e.g. slight opacity or scale).
    - Ensure dragging works correctly with the slide scaling (calculated in `SlideScale.client.ts`). You may need to divide the delta by the current scale factor.
  </action>
  <verify>
    <automated>grep "cursor" packages/core/src/components/SDrag.client.ts || grep "scale" packages/core/src/components/SDrag.client.ts</automated>
  </verify>
  <done>Dragging interaction is polished and handles slide scaling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → File System | Indirect writing to the file system via WebSocket bridge. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-03-01 | Denial of Service | packages/core/src/index.ts | mitigate | Rate limit file writes or use atomic write operations to prevent file corruption from rapid updates. |
</threat_model>

<verification>
Run the Cypress test suite: `pnpm cypress run --spec cypress/e2e/persistence.cy.ts`.
</verification>

<success_criteria>
1. Dragging an element updates the source file.
2. The update is surgical and doesn't affect other parts of the Markdown.
3. The new position is maintained after a full page reload.
4. Dragging handles slide scaling correctly (no "drifting").
</success_criteria>

<output>
After completion, create `.planning/phases/17-interactive-persistence/17-03-SUMMARY.md`
</output>
