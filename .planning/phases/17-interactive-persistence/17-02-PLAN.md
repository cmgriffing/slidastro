---
phase: 17
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified: [packages/core/package.json, packages/core/src/utils/markdown.ts, packages/core/src/virtual.ts, packages/core/src/index.ts]
autonomous: true
requirements: [INT-02]

must_haves:
  truths:
    - "Server can receive position updates via WebSocket."
    - "Server can identify the correct s-drag tag in the source file."
  artifacts:
    - path: "packages/core/src/utils/markdown.ts"
      provides: "Shared utility for slide splitting and tag finding"
    - path: "packages/core/src/index.ts"
      provides: "WebSocket listener for position updates"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/utils/markdown.ts"
      via: "import for file manipulation"
---

<objective>
Setup the persistence infrastructure including the WebSocket listener and surgical file update logic.

Purpose: Enable the server to save updated element positions back to the source Markdown.
Output: Server-side logic for updating `<s-drag>` tags in files.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/virtual.ts
@packages/core/src/index.ts
@.planning/phases/17-interactive-persistence/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add magic-string and create markdown utility</name>
  <files>packages/core/package.json, packages/core/src/utils/markdown.ts, packages/core/src/virtual.ts</files>
  <action>
    - Add `magic-string` to `packages/core/package.json` dependencies.
    - Create `packages/core/src/utils/markdown.ts`.
    - Move `splitSlides` logic from `virtual.ts` to `markdown.ts` and export it.
    - Refactor `virtual.ts` to use the exported `splitSlides`.
  </action>
  <verify>
    <automated>grep "magic-string" packages/core/package.json && ls packages/core/src/utils/markdown.ts</automated>
  </verify>
  <done>Infrastructure for file manipulation is ready.</done>
</task>

<task type="auto">
  <name>Task 2: Implement server-side position update listener</name>
  <files>packages/core/src/index.ts</files>
  <action>
    - In `astro:server:setup`, add a listener for `slidastro:update-pos`.
    - The listener should receive `{ filepath, slideIndex, dragId, x, y }`.
    - It should:
      1. Read the file content.
      2. Use `splitSlides` to find the slide content.
      3. Find the `dragId`-th `<s-drag>` tag within that slide.
      4. Use `magic-string` to surgically update the `x` and `y` attributes.
      5. Write the updated content back to the file.
    - Ensure it only runs in dev mode (`server.config.command === 'serve'`).
  </action>
  <verify>
    <automated>grep "slidastro:update-pos" packages/core/src/index.ts</automated>
  </verify>
  <done>Server can surgically update Markdown files based on drag events.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server (WebSocket) | Untrusted position data and file path received from client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-02-01 | Elevation of Privilege | packages/core/src/index.ts | mitigate | Validate that `filepath` is within the project root and is a supported extension (.md, .astro, .mdx). |
| T-17-02-02 | Information Disclosure | packages/core/src/index.ts | mitigate | Ensure the listener is only active during dev mode (`astro dev`). |
</threat_model>

<verification>
Automated check: Verify that the `astro:server:setup` hook contains the `slidastro:update-pos` listener.
</verification>

<success_criteria>
1. Server receives `slidastro:update-pos` messages.
2. Server correctly identifies the target `<s-drag>` tag using slide index and drag index.
3. Server uses `magic-string` to update attributes without breaking other content.
</success_criteria>

<output>
After completion, create `.planning/phases/17-interactive-persistence/17-02-SUMMARY.md`
</output>
