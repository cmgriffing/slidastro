---
phase: 10-analysis-normal-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/core/src/templates/SlideView.astro, packages/core/src/components/SlideContainer.astro, packages/core/src/components/SlideScale.client.ts]
autonomous: true
requirements: [LAYOUT-01]
user_setup: []

must_haves:
  truths:
    - "Slides are scaled to fit the viewport without clipping"
    - "The slide maintains its aspect ratio correctly"
    - "The background surrounding the slide has a professional stage-like appearance"
  artifacts:
    - path: "packages/core/src/components/SlideScale.client.ts"
      provides: "New scaling logic that scales the entire container"
    - path: "packages/core/src/templates/SlideView.astro"
      provides: "Stage wrapper and updated background styles"
  key_links:
    - from: "packages/core/src/templates/SlideView.astro"
      to: "packages/core/src/components/SlideScale.client.ts"
      via: "initSlideScale() call"
---

<objective>
Refine the slide scaling and centering logic to ensure slides fit the viewport perfectly while maintaining aspect ratio and presenting a professional "stage" background.

Purpose: Correcting the current clipped scaling and providing a better audience experience in normal mode.
Output: Updated scaling engine and slide shell components.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/templates/SlideView.astro
@packages/core/src/components/SlideScale.client.ts
@packages/core/src/components/SlideContainer.astro
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update SlideContainer structure and styles</name>
  <files>packages/core/src/components/SlideContainer.astro</files>
  <action>
    - Ensure `.slide-container` does not use `overflow: hidden` by default if it clips its children when scaled. Actually, we want it to clip its children, but we want the *container* itself to be scaled.
    - Keep `width` and `height` from props.
    - Add `transform-origin: center center` to `.slide-container` to facilitate centering via scale + translate or flexbox.
  </action>
  <verify>
    <automated>grep "transform-origin: center center" packages/core/src/components/SlideContainer.astro</automated>
  </verify>
  <done>SlideContainer is prepared for scaling.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Stage wrapper and Stage styles</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    - Wrap `<SlideContainer>` in a `<div class="stage">`.
    - Update `body` styles: `background: #050505; display: flex; align-items: center; justify-content: center; overflow: hidden; height: 100vh; width: 100vw;`.
    - Style `.stage` to have a radial gradient background for a "stage" effect: `background: radial-gradient(circle, #222 0%, #050505 100%); flex: 1; display: flex; align-items: center; justify-content: center;`.
    - Remove fixed `#f0f0f0` background from body.
  </action>
  <verify>
    <automated>grep "class=\"stage\"" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>The professional stage background is implemented.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Refactor scaling logic to scale the container</name>
  <files>packages/core/src/components/SlideScale.client.ts</files>
  <action>
    - Update `initSlideScale` to target the `.slide-container` for scaling.
    - Remove scaling from `#slide-content-wrapper` (inner content).
    - Update `update` function:
      1. Calculate `scale = Math.min(width / canvasWidth, height / canvasHeight)`.
      2. Apply `transform: scale(${scale})` to the container.
      3. Since the container is inside a flexbox stage, we don't need manual `left`/`top` offsets if we use `transform: scale()`.
      4. However, to avoid layout shifts and ensure pixel-perfect centering, consider using `fixed` positioning with `top: 50%; left: 50%; transform: translate(-50%, -50%) scale(${scale});`.
    - Ensure the logic works correctly on window resize.
  </action>
  <verify>
    <automated>grep "scale(${scale})" packages/core/src/components/SlideScale.client.ts</automated>
  </verify>
  <done>The scaling logic now scales the entire slide container correctly.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Centering and Scaling engine refactored with professional stage background.</what-built>
  <how-to-verify>
    1. Run `pnpm dev` in a slide project.
    2. Visit a slide (e.g., http://localhost:4321/1).
    3. Resize the browser window.
    4. Confirm the slide stays centered and scales to fit the viewport without being clipped.
    5. Confirm the background looks professional (darker, stage-like).
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Browser | Scale calculations happen on client side based on viewport dimensions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | R | SlideScale.client.ts | mitigate | Use requestAnimationFrame for smooth scaling on resize and avoid layout thrashing. |
</threat_model>

<verification>
Ensure slides are centered and scaled on various screen sizes (Mobile, Tablet, Desktop) without clipping.
</verification>

<success_criteria>
1. Slide container is scaled to fit the window.
2. No content is clipped.
3. Background is dark with stage effect.
</success_criteria>

<output>
After completion, create `.planning/phases/10-analysis-normal-mode/10-01-SUMMARY.md`
</output>
