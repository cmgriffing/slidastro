---
phase: 10-analysis-normal-mode
plan: 02
type: execute
wave: 2
depends_on: [10-01]
files_modified: [packages/core/src/layouts/*.astro]
autonomous: true
requirements: [LAYOUT-06]
user_setup: []

must_haves:
  truths:
    - "All 15+ built-in layouts render correctly within the new scaled container"
    - "Layout content fills the available slide space as expected"
  artifacts:
    - path: "packages/core/src/layouts/default.astro"
      provides: "Reference for layout content fitting"
  key_links: []
---

<objective>
Verify that all 15+ built-in layouts render correctly and utilize the full available slide space after the scaling logic update.

Purpose: Ensure consistent visual quality across all slide types and aspect ratios.
Output: Audited and potentially patched built-in layouts.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-analysis-normal-mode/10-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Audit all layouts for full-space utilization</name>
  <files>packages/core/src/layouts/center.astro, packages/core/src/layouts/cover.astro, packages/core/src/layouts/default.astro, packages/core/src/layouts/end.astro, packages/core/src/layouts/fact.astro, packages/core/src/layouts/iframe-left.astro, packages/core/src/layouts/iframe-right.astro, packages/core/src/layouts/iframe.astro, packages/core/src/layouts/image-left.astro, packages/core/src/layouts/image-right.astro, packages/core/src/layouts/image.astro, packages/core/src/layouts/quote.astro, packages/core/src/layouts/section.astro, packages/core/src/layouts/statement.astro, packages/core/src/layouts/two-cols.astro</files>
  <action>
    - Review each `.astro` file in `packages/core/src/layouts/`.
    - Ensure the top-level div in each layout has `h-full` (to fill the slide height).
    - Check if any hardcoded margins or paddings break the "full available space" goal.
    - If `h-full` is missing, add it.
  </action>
  <verify>
    <automated>grep "h-full" packages/core/src/layouts/*.astro | wc -l</automated>
  </verify>
  <done>All layouts are confirmed to fill the available space.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all 15 layouts</name>
  <what-built>Verified visual parity and space utilization for all layouts.</what-built>
  <how-to-verify>
    1. Create a demo presentation with 15+ slides, each using a different layout.
    2. Start the dev server.
    3. Cycle through all slides.
    4. Confirm each slide:
       - Fills the space correctly (no unintended margins).
       - Is centered (inherited from Plan 01).
       - Content is readable and correctly positioned.
    5. Test with different browser aspect ratios.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

N/A - Layout rendering is purely visual/presentation.

## STRIDE Threat Register

N/A - No security implications for layout CSS.
</threat_model>

<verification>
Check all 15 layouts for visual correctness.
</verification>

<success_criteria>
1. All 15 layouts use the full slide area.
2. Centering works perfectly for all layouts.
3. Aspect ratio is respected.
</success_criteria>

<output>
After completion, create `.planning/phases/10-analysis-normal-mode/10-02-SUMMARY.md`
</output>
