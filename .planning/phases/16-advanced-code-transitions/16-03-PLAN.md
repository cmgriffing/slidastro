---
phase: 16-advanced-code-transitions
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified: [packages/core/src/styles/magic-move.css, packages/core/src/templates/SlideView.astro, tests/fixtures/magic-move.md, cypress/e2e/magic-move.cy.ts]
autonomous: false
requirements: [VIS-01]

must_haves:
  truths:
    - "Transitions between code states are smooth and visually appealing"
    - "Themes (light/dark) are respected in code transitions"
    - "Custom durations and options are correctly applied"
  artifacts:
    - path: "packages/core/src/styles/magic-move.css"
      provides: "Visual styling for MagicMove"
    - path: "tests/fixtures/magic-move.md"
      provides: "Test case for verification"
    - path: "cypress/e2e/magic-move.cy.ts"
      provides: "E2E verification of animations"
---

<objective>
Apply final visual styling, create integration demo, and verify the implementation with E2E tests.

Purpose: To ensure the feature is visually polished, works across all slide formats, and is stable under real-world usage.
Output: CSS styles, a demo markdown file, and Cypress tests.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@packages/core/src/styles/layouts-base.css
@cypress/e2e/navigation.cy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MagicMove CSS styles</name>
  <files>packages/core/src/styles/magic-move.css, packages/core/src/templates/SlideView.astro</files>
  <action>
    1. Create `packages/core/src/styles/magic-move.css` with necessary styles for Shiki Magic Move:
       - `.shiki-magic-move-container` styles (overflow: hidden, position: relative).
       - `.shiki-magic-move-tokens` transition properties.
       - Token specific styles (color, font-family, line-height).
       - Ensure themes (vitesse-light/dark) are properly integrated.
    2. Import `magic-move.css` in `packages/core/src/templates/SlideView.astro`.
  </action>
  <verify>
    <automated>grep "@shikijs/markdown-it" packages/core/package.json</automated>
  </verify>
  <done>Styling added and imported into SlideView.</done>
</task>

<task type="auto">
  <name>Task 2: Create demo slides</name>
  <files>tests/fixtures/magic-move.md</files>
  <action>
    Create a markdown file `tests/fixtures/magic-move.md` containing a `magic-move` block with multiple steps to showcase the feature:
    ```md
    # Magic Move Demo

    ````md magic-move {duration: 800}
    ```ts
    const a = 1;
    ```
    ```ts
    const a = 1;
    const b = 2;
    ```
    ```ts
    function add(a, b) {
      return a + b;
    }
    ```
    ````
    ```
  </action>
  <verify>
    <automated>ls tests/fixtures/magic-move.md</automated>
  </verify>
  <done>Demo slides created for integration testing.</done>
</task>

<task type="auto">
  <name>Task 3: E2E Tests with Cypress</name>
  <files>cypress/e2e/magic-move.cy.ts</files>
  <action>
    Create `cypress/e2e/magic-move.cy.ts`:
    1. Load the `magic-move.md` demo.
    2. Verify the initial state of the magic move container.
    3. Click to advance steps.
    4. Assert that the container's content changes correctly.
    5. (Optional) Check for presence of shiki-magic-move internal classes during transition.
  </action>
  <verify>
    <automated>pnpm exec cypress run --spec cypress/e2e/magic-move.cy.ts</automated>
  </verify>
  <done>E2E tests verify the full functionality.</done>
</task>

<task type="checkpoint:human-verify">
  <what-built>Advanced Code Transitions with Shiki Magic Move</what-built>
  <how-to-verify>
    1. Run `pnpm dev tests/fixtures/magic-move.md`.
    2. Open the browser and navigate through the steps.
    3. Verify that the code transitions are smooth and correctly themed.
    4. Check both light and dark modes.
  </how-to-verify>
  <resume-signal>Approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| CSS -> DOM | Styles are applied to the generated magic-move elements. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure | Code in tokens | mitigate | Shiki tokenization only includes public code structure and tokens. |
</threat_model>

<success_criteria>
1. All Cypress tests pass.
2. Code transitions are visually smooth and follow the expected click sequence.
3. Theming works correctly for the code blocks.
</success_criteria>

<output>
After completion, create `.planning/phases/16-advanced-code-transitions/16-03-SUMMARY.md`
</output>
