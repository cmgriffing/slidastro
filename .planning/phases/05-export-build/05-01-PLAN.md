---
phase: 05-export-build
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/templates/PrintView.astro
  - packages/core/src/index.ts
  - packages/cli/src/build.ts
  - packages/cli/src/export.ts
  - packages/cli/src/index.ts
  - packages/cli/package.json
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04]

must_haves:
  truths:
    - "slidastro build produces a deployable static SPA in dist/"
    - "slidastro export produces a high-fidelity PDF by default"
    - "slidastro export --format png produces individual slide images"
    - "/print route displays all slides for capture"
  artifacts:
    - path: "packages/core/src/templates/PrintView.astro"
      provides: "Printable view of all slides"
    - path: "packages/cli/src/build.ts"
      provides: "Static build implementation using Astro API"
    - path: "packages/cli/src/export.ts"
      provides: "PDF/PNG export implementation using Playwright"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/build.ts"
      via: "import and call build()"
    - from: "packages/cli/src/index.ts"
      to: "packages/cli/src/export.ts"
      via: "import and call export()"
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/templates/PrintView.astro"
      via: "injectRoute as /print"
---

<objective>
Consolidate Phase 5 into a single execution plan to implement production builds and high-fidelity exports.

Purpose: Deliver the 'Export & Build' phase which enables sharing presentations via static hosting or PDF/PNG files.
Output: Working 'build' and 'export' commands in the slidastro CLI.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-export-build/SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Core Printing Infrastructure</name>
  <files>packages/core/src/templates/PrintView.astro, packages/core/src/index.ts</files>
  <action>
    Create a new template `PrintView.astro` that renders all slides sequentially in a vertical stack.
    - Import `slidesData` from `virtual:slidastro/slides`.
    - Iterate over `slidesData.slides`.
    - Handle both built-in and theme/custom layouts (reuse logic from `SlideView.astro`).
    - Add `@media print` CSS to ensure each slide is on a new page (`page-break-after: always`).
    - Ensure slides are scaled to fit the print canvas (default 16:9).
    
    Inject the `/print` route in `packages/core/src/index.ts` pointing to this new template.
  </action>
  <verify>
    <automated>
      # Start dev server in background and check if /print responds
      # Note: This is a simplified check. Real verification happens in Task 3.
      grep "/print" packages/core/src/index.ts
    </automated>
  </verify>
  <done>/print route is injected and template exists.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Build Command</name>
  <files>packages/cli/src/build.ts, packages/cli/src/index.ts</files>
  <action>
    Implement the `build` function in `packages/cli/src/build.ts` using Astro's programmatic `build` API.
    - Import `build` from `astro`.
    - Use `slidastroIntegration({ entry })` in the build configuration.
    - Support `--outDir` option (defaulting to `dist`).
    - Wire the command in `packages/cli/src/index.ts`.
  </action>
  <verify>
    <automated>
      # Assuming a test slides.md exists from previous phases
      npx tsx packages/cli/src/index.ts build tests/slides.md --outDir test-dist
      test -d test-dist && test -f test-dist/index.html
    </automated>
  </verify>
  <done>`slidastro build` produces a valid static site in the output directory.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Export Command</name>
  <files>packages/cli/src/export.ts, packages/cli/src/index.ts, packages/cli/package.json</files>
  <action>
    Add `playwright-core` and `ansis` to `packages/cli/package.json` dependencies.
    Implement the `export` function in `packages/cli/src/export.ts`.
    - Use `playwright-core` to launch Chromium.
    - It should:
      1. Start a temporary Astro dev server (or use programmatic `preview` after build).
      2. Visit `http://localhost:[port]/print`.
      3. Wait for `networkidle` and all async content (use a small timeout or Slidev's wait patterns if possible).
      4. For PDF: use `page.pdf()` with `preferCSSPageSize: true`.
      5. For PNG: capture individual screenshots of each slide container.
    - Handle format detection from CLI options.
    - Wire the command in `packages/cli/src/index.ts`.
  </action>
  <verify>
    <automated>
      # Verify the command exists and playwright is integrated
      npx tsx packages/cli/src/index.ts export --help | grep "Export"
    </automated>
  </verify>
  <done>`slidastro export` generates PDF/PNG files that match the presentation content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File System -> CLI | CLI reads local markdown files provided by user |
| CLI -> Playwright | CLI passes local URL to headless browser |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | slides.md | accept | User provides the file they want to build/export |
| T-05-02 | Denial of Service | Playwright | mitigate | Add timeout to Playwright operations (default 30s) |
| T-05-03 | Information Disclosure | Build Artifacts | accept | Build output is intended to be public static files |
</threat_model>

<verification>
1. Run `slidastro build slides.md` and verify `dist/index.html` contains slide content.
2. Run `slidastro export slides.md` and verify `slides.pdf` exists and is readable.
3. Run `slidastro export slides.md --format png` and verify PNG files are generated.
</verification>

<success_criteria>
- Build command produces working static SPA.
- Export command produces high-fidelity PDF and PNG files.
- /print route enables easy multi-slide capture.
</success_criteria>

<output>
After completion, create `.planning/phases/05-export-build/05-01-SUMMARY.md`
</output>
