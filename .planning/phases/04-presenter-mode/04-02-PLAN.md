---
phase: 04-presenter-mode
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: [packages/core/src/index.ts, packages/core/src/templates/PresenterView.astro, packages/core/src/components/PresenterLayout.astro, packages/core/src/components/SlidePreview.astro]
autonomous: false
requirements: [PRES-01, PRES-02]

must_haves:
  truths:
    - "Navigating to /presenter/1 displays the presenter view"
    - "Presenter view shows speaker notes if defined in the slide"
    - "Presenter view shows a preview of the current and next slides"
  artifacts:
    - path: "packages/core/src/templates/PresenterView.astro"
      provides: "Presenter view route handler"
    - path: "packages/core/src/components/PresenterLayout.astro"
      provides: "Presenter-specific layout"
    - path: "packages/core/src/components/SlidePreview.astro"
      provides: "Scaled slide preview component"
  key_links:
    - from: "packages/core/src/index.ts"
      to: "packages/core/src/templates/PresenterView.astro"
      via: "injectRoute()"
---

<objective>
Implement the Presenter View route, layout, and speaker notes display.

Purpose: Provide the user with a secondary "presenter" view that displays private notes and next-slide previews.
Output: Working /presenter route and UI.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/index.ts
@packages/core/src/templates/SlideView.astro
@packages/core/src/virtual.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Presenter Route</name>
  <files>packages/core/src/index.ts</files>
  <action>
    - Register `/presenter/[...no]` route in `astro:config:setup`.
    - Point it to `templates/PresenterView.astro`.
  </action>
  <verify>
    <automated>grep "/presenter/\[...no\]" packages/core/src/index.ts</automated>
  </verify>
  <done>The presenter route is registered with Astro.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Slide Preview Component</name>
  <files>packages/core/src/components/SlidePreview.astro</files>
  <action>
    - Create `SlidePreview.astro`.
    - This component should render a scaled-down version of a slide.
    - Props: `index` (slide number), `scale` (optional).
    - Use `SlideContainer` internally with fixed dimensions.
    - Set `pointer-events: none` to prevent interaction in previews.
  </action>
  <verify>
    <automated>ls packages/core/src/components/SlidePreview.astro</automated>
  </verify>
  <done>Slide preview component is ready for use.</done>
</task>

<task type="auto">
  <name>Task 3: Create Presenter View and Layout</name>
  <files>packages/core/src/templates/PresenterView.astro, packages/core/src/components/PresenterLayout.astro</files>
  <action>
    - Implement `PresenterLayout.astro` for the presenter shell.
    - Implement `PresenterView.astro`.
    - Create a 3-column layout:
      - Main: Current slide preview.
      - Right top: Next slide preview.
      - Right bottom: Speaker notes (`noteHTML` from `slidesData`).
    - Add navigation buttons (Prev/Next).
    - Synchronize with Nano Stores (already implemented in Plan 01).
  </action>
  <verify>
    <automated>ls packages/core/src/templates/PresenterView.astro</automated>
  </verify>
  <done>Presenter UI is implemented and functional.</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verify Presenter View</name>
  <what-built>Presenter View at /presenter/1</what-built>
  <how-to-verify>
    1. Open `http://localhost:4321/presenter/1`.
    2. Verify you see the current slide, the next slide preview, and speaker notes (if defined).
    3. Click "Next"; verify both previews update and the notes change.
  </how-to-verify>
  <resume-signal>approved</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server→Client | Note HTML is rendered on the server and injected into the client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | X | noteHTML | mitigate | Ensure Shiki/KaTeX rendering handles sanitization. Note: Shiki/KaTeX are generally safe but input is untrusted. |
</threat_model>

<verification>
1. Navigate to `/presenter/1`.
2. Verify visual presence of notes and next slide preview.
</verification>

<success_criteria>
- The `/presenter/[...no]` route correctly displays a synchronized presenter interface.
</success_criteria>

<output>
After completion, create `.planning/phases/04-presenter-mode/04-02-SUMMARY.md`
</output>
