---
phase: 04-presenter-mode
plan: 03
type: execute
wave: 3
depends_on: ["02"]
files_modified: [packages/client/src/state.ts, packages/client/src/sync.ts, packages/core/src/index.ts, packages/core/src/templates/PresenterView.astro]
autonomous: true
requirements: [PRES-03, PRES-05]

must_haves:
  truths:
    - "Presentation timer tracks and displays total duration"
    - "WebSocket connection is established and relays state between windows"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "WebSocket relay server"
    - path: "packages/client/src/state.ts"
      provides: "Timer store"
  key_links:
    - from: "packages/client/src/sync.ts"
      to: "WebSocket Server"
      via: "new WebSocket(url)"
---

<objective>
Implement the presentation timer and WebSocket synchronization bridge for dev-mode.

Purpose: Provide time tracking and enable remote (cross-network) synchronization during development.
Output: Synced timer and WebSocket bridge.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/index.ts
@packages/client/src/state.ts
@packages/client/src/sync.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Timer state</name>
  <files>packages/client/src/state.ts</files>
  <action>
    - Add `timer` store to `state.ts`.
    - `timer` object: `{ status: 'stopped' | 'running' | 'paused'; elapsed: number; lastStarted: number }`.
    - Implement a `startTimer()`, `pauseTimer()`, and `resetTimer()` functions that update the store.
    - Persist `elapsed` in `localStorage` for continuity.
  </action>
  <verify>
    <automated>grep "timer" packages/client/src/state.ts</automated>
  </verify>
  <done>Timer state is managed by a Nano Store.</done>
</task>

<task type="auto">
  <name>Task 2: Implement WebSocket Server relay</name>
  <files>packages/core/src/index.ts</files>
  <action>
    - Update `astro:server:setup` hook to attach a simple WebSocket server to the Vite instance.
    - The server should broadcast any received message to all other connected clients.
    - Type-safe messages: `page`, `clicks`, `timer`.
  </action>
  <verify>
    <automated>grep "server.ws" packages/core/src/index.ts</automated>
  </verify>
  <done>WebSocket server relay is implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Implement WebSocket Client bridge</name>
  <files>packages/client/src/sync.ts, packages/core/src/templates/PresenterView.astro</files>
  <action>
    - Update `sync.ts` to also connect to the WebSocket server in dev mode.
    - Synchronize Nano Stores via WebSocket (similar to the BroadcastChannel logic).
    - Add a timer UI component to `PresenterView.astro` that consumes the `timer` store.
    - Show Reset/Pause/Resume buttons in the presenter toolbar.
  </action>
  <verify>
    <automated>grep "socket" packages/client/src/sync.ts</automated>
  </verify>
  <done>WebSocket client and Timer UI are functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client↔Server | Synchronization messages are sent over WebSockets in dev mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | D | WebSocket Relay | mitigate | The relay only exists in dev mode. Limit broadcast frequency to prevent accidental DDoS. |
</threat_model>

<verification>
1. Start `slidastro dev`.
2. Open Audience View and Presenter View.
3. Start the timer in Presenter View; verify it ticks in both.
4. Verify navigation sync works between windows.
</verification>

<success_criteria>
- The presentation timer is synced and persistent.
- WebSocket bridge enables state relay between windows.
</success_criteria>

<output>
After completion, create `.planning/phases/04-presenter-mode/04-03-SUMMARY.md`
</output>
