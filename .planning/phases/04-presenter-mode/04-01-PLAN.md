---
phase: 04-presenter-mode
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/client/package.json, packages/client/src/state.ts, packages/client/src/sync.ts, packages/core/src/components/Navigation.client.ts]
autonomous: true
requirements: [PRES-01, PRES-04]

must_haves:
  truths:
    - "Navigation state (page, clicks) is managed by Nano Stores"
    - "State changes in one tab are reflected in other tabs via BroadcastChannel"
  artifacts:
    - path: "packages/client/src/state.ts"
      provides: "Nano Stores for shared state"
    - path: "packages/client/src/sync.ts"
      provides: "BroadcastChannel sync bridge"
  key_links:
    - from: "packages/core/src/components/Navigation.client.ts"
      to: "packages/client/src/state.ts"
      via: "store.set() on navigation"
---

<objective>
Implement a robust synchronization layer using Nano Stores and `BroadcastChannel` to keep multiple Slidastro windows in sync.

Purpose: Ensure the presenter view and viewer view stay perfectly synchronized across same-origin tabs.
Output: Shared state stores and a working sync bridge.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/components/Navigation.client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add nanostores and create state stores</name>
  <files>packages/client/package.json, packages/client/src/state.ts</files>
  <action>
    - Add `nanostores` dependency to `packages/client/package.json`.
    - Create `packages/client/src/state.ts`.
    - Export stores for:
      - `page`: Current slide number (1-indexed).
      - `clicks`: Current click index within the slide.
      - `clicksTotal`: Total clicks available on current slide.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/client install && ls packages/client/src/state.ts</automated>
  </verify>
  <done>Nano Stores are defined and ready for use.</done>
</task>

<task type="auto">
  <name>Task 2: Implement BroadcastChannel sync bridge</name>
  <files>packages/client/src/sync.ts</files>
  <action>
    - Create `packages/client/src/sync.ts`.
    - Use `BroadcastChannel('slidastro')` to listen for and emit state updates.
    - Subscribe to Nano Stores and broadcast changes.
    - Update Nano Stores when receiving messages from the channel.
    - Prevent infinite loops (don't broadcast received updates).
  </action>
  <verify>
    <automated>grep "BroadcastChannel" packages/client/src/sync.ts</automated>
  </verify>
  <done>Same-origin tab sync is implemented via BroadcastChannel.</done>
</task>

<task type="auto">
  <name>Task 3: Integrate stores into Navigation</name>
  <files>packages/core/src/components/Navigation.client.ts</files>
  <action>
    - Import stores from `@slidastro/client/state`.
    - Update `initNavigation` to:
      - Synchronize local `currentClick` with the `clicks` store.
      - Update `page` and `clicks` stores when user navigates.
      - Subscribe to store changes to trigger `navigate()` or `applyClicks()`.
  </action>
  <verify>
    <automated>grep "store" packages/core/src/components/Navigation.client.ts</automated>
  </verify>
  <done>Navigation actions now propagate through Nano Stores.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab→Tab | Synchronization data crosses between tabs via BroadcastChannel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | S | BroadcastChannel | accept | BroadcastChannel is restricted to the same origin by the browser. |
| T-04-02 | I | State Stores | mitigate | Validate that `page` and `clicks` are non-negative integers. |
</threat_model>

<verification>
1. Open http://localhost:4321/1 in two tabs.
2. Advance in Tab 1; verify Tab 2 follows.
3. Advance click in Tab 2; verify Tab 1 follows.
</verification>

<success_criteria>
- Navigation state is unified across tabs via Nano Stores and BroadcastChannel.
</success_criteria>

<output>
After completion, create `.planning/phases/04-presenter-mode/04-01-SUMMARY.md`
</output>
