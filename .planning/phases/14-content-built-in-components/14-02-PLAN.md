---
phase: 14-content-built-in-components
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified: [packages/core/src/components/STweet.astro, packages/core/src/components/SYoutube.astro, packages/core/src/components/SVideo.astro]
autonomous: true
requirements: [CONT-02, CONT-03]

must_haves:
  truths:
    - "User can embed Tweets using <s-tweet id=\"...\" />"
    - "User can embed YouTube videos using <s-youtube id=\"...\" />"
    - "User can embed HTML5 videos using <s-video src=\"...\" />"
    - "Media (YouTube/Video) pauses when the slide is changed"
  artifacts:
    - path: "packages/core/src/components/STweet.astro"
      provides: "Tweet embed component"
    - path: "packages/core/src/components/SYoutube.astro"
      provides: "YouTube embed component"
    - path: "packages/core/src/components/SVideo.astro"
      provides: "HTML5 video component"
  key_links:
    - from: "packages/core/src/components/SYoutube.astro"
      to: "@slidastro/client"
      via: "import { $page } from '@slidastro/client'"
---

<objective>
Implement social and media embed components: STweet, SYoutube, and SVideo.
Include media synchronization logic to play/pause content based on slide visibility.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@packages/core/package.json
@packages/client/src/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement STweet component</name>
  <files>packages/core/src/components/STweet.astro</files>
  <action>
    Create `STweet.astro` using `@astro-community/astro-embed-twitter`.
    - Accept `id` prop.
    - Provide a wrapper for the embed component.
    - Style to fit within the slide container.
  </action>
  <verify>
    <automated>test -f packages/core/src/components/STweet.astro</automated>
  </verify>
  <done>STweet component implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement SYoutube component with media sync</name>
  <files>packages/core/src/components/SYoutube.astro</files>
  <action>
    Create `SYoutube.astro` using `@astro-community/astro-embed-youtube`.
    - Accept `id` and `autoplay` props.
    - Add a client-side script that:
        - Finds its own slide index via `closest('.content-wrapper').dataset.index`.
        - Subscribes to `$page` from `@slidastro/client`.
        - Pauses the video if the slide is no longer active.
        - (Optional) Plays the video if `autoplay` is true and slide becomes active.
    - Note: Might need YouTube IFrame API or lite-youtube hooks for programmatic pause.
  </action>
  <verify>
    <automated>test -f packages/core/src/components/SYoutube.astro</automated>
  </verify>
  <done>SYoutube component implemented with slide synchronization</done>
</task>

<task type="auto">
  <name>Task 3: Implement SVideo component with media sync</name>
  <files>packages/core/src/components/SVideo.astro</files>
  <action>
    Create `SVideo.astro` using native `<video>` tag.
    - Accept `src`, `autoplay`, `loop`, `controls`, `muted`.
    - Add a client-side script similar to SYoutube that plays/pauses the video element based on `$page` state.
  </action>
  <verify>
    <automated>test -f packages/core/src/components/SVideo.astro</automated>
  </verify>
  <done>SVideo component implemented with slide synchronization</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content -> Browser | SYoutube/STweet fetch content from external third-party domains (Google, X) |
| Local Files -> Browser | SVideo plays local or remote video files |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-03 | Spoofing | SYoutube/STweet | accept | Relies on third-party embed security. |
| T-14-04 | Denial of Service | SVideo | mitigate | Ensure video files are not excessively large or use streaming if possible. |
</threat_model>

<verification>
1. Verify STweet renders a tweet embed.
2. Verify SYoutube renders a video and pauses when moving to a different slide.
3. Verify SVideo renders and synchronizes with slide changes.
</verification>

<success_criteria>
- Tweets and YouTube videos can be embedded.
- Local/Remote videos can be embedded.
- Media synchronization works correctly across slide transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-built-in-components/14-02-SUMMARY.md`
</output>
