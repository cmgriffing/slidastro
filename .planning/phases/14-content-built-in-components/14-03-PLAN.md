---
phase: 14-content-built-in-components
plan: 03
type: execute
wave: 3
depends_on: [14-02]
files_modified: [cypress/e2e/components.cy.ts, demo.md]
autonomous: true
requirements: [CONT-01, CONT-02, CONT-03, CONT-04]

must_haves:
  truths:
    - "All built-in components are verified to work in a real presentation"
    - "Automated tests cover component existence and basic functionality"
  artifacts:
    - path: "cypress/e2e/components.cy.ts"
      provides: "E2E tests for content components"
  key_links: []
---

<objective>
Verify all Phase 14 components through a demo presentation and automated Cypress tests.
Ensure the media synchronization logic is robust and components render correctly in different slide formats (.md, .astro, .mdx).
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@demo.md
@cypress/e2e/clicks.cy.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create demo presentation with all components</name>
  <files>demo.md</files>
  <action>
    Update or create a `demo.md` (or use an existing one) that includes:
    - A slide with `<s-toc />`.
    - A slide with `<s-link to="1">Back to Start</s-link>` and `<s-link href="https://google.com">Google</s-link>`.
    - A slide with `<s-tweet id="123456789" />` (use a real public tweet ID).
    - A slide with `<s-youtube id="dQw4w9WgXcQ" />`.
    - A slide with `<s-video src="https://www.w3schools.com/html/mov_bbb.mp4" />`.
  </action>
  <verify>
    <automated>grep -E "<s-(toc|link|tweet|youtube|video)" demo.md</automated>
  </verify>
  <done>Demo presentation created with all new components</done>
</task>

<task type="auto">
  <name>Task 2: Implement Cypress E2E tests for components</name>
  <files>cypress/e2e/components.cy.ts</files>
  <action>
    Create `cypress/e2e/components.cy.ts` to:
    - Verify TOC links exist and have correct hrefs.
    - Verify SLink navigates correctly.
    - Verify Tweet, YouTube, and Video elements are present in the DOM.
    - Verify that navigating away from a video slide pauses the video (if possible to detect via DOM/State).
  </action>
  <verify>
    <automated>pnpm cypress run --spec cypress/e2e/components.cy.ts</automated>
  </verify>
  <done>Automated tests implemented and passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Environment -> Browser | Cypress controls the browser to verify behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-05 | Information Disclosure | Tests | mitigate | Ensure no sensitive IDs or URLs are used in public test suites. |
</threat_model>

<verification>
1. Run `pnpm run slidastro dev demo.md` and manually verify components.
2. Run `pnpm cypress run --spec cypress/e2e/components.cy.ts` to ensure automated verification passes.
</verification>

<success_criteria>
- All Phase 14 components are verified functional.
- Automated tests pass.
- Media synchronization is confirmed.
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-built-in-components/14-03-SUMMARY.md`
</output>
