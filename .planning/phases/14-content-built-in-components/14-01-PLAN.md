---
phase: 14-content-built-in-components
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/core/package.json, packages/core/src/components/SLink.astro, packages/core/src/components/SToc.astro, packages/core/src/renderer.ts]
autonomous: true
requirements: [CONT-01, CONT-04]

must_haves:
  truths:
    - "User can use <s-link> for internal and external navigation"
    - "User can use <s-toc> to display a clickable table of contents"
  artifacts:
    - path: "packages/core/src/components/SLink.astro"
      provides: "Internal/External link component"
    - path: "packages/core/src/components/SToc.astro"
      provides: "Table of Contents component"
  key_links:
    - from: "packages/core/src/components/SToc.astro"
      to: "virtual:slidastro/slides"
      via: "import"
---

<objective>
Install core dependencies for Phase 14 and implement the foundation structural components: SLink and SToc.
Ensure these components are correctly handled in the Markdown rendering pipeline.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@packages/core/package.json
@packages/core/src/renderer.ts
@packages/core/src/virtual.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Setup dependencies and boilerplate</name>
  <files>packages/core/package.json</files>
  <action>
    Install `@astro-community/astro-embed-twitter` and `@astro-community/astro-embed-youtube` in `packages/core`.
    These will be used in the next plan.
  </action>
  <verify>
    <automated>grep -E "@astro-community/astro-embed-(twitter|youtube)" packages/core/package.json</automated>
  </verify>
  <done>Dependencies added to core package.json</done>
</task>

<task type="auto">
  <name>Task 2: Implement SLink component</name>
  <files>packages/core/src/components/SLink.astro</files>
  <action>
    Create `SLink.astro` that accepts `href` or `to`.
    - If `to` is provided:
        - If it's a number, link to `/${to}`.
        - If it's "next" or "prev", use a client-side click handler to navigate using `@slidastro/client` state.
    - If `href` is provided, render a standard `<a>` tag.
    - Add default styling consistent with Slidev links.
  </action>
  <verify>
    <automated>test -f packages/core/src/components/SLink.astro</automated>
  </verify>
  <done>SLink component implemented with internal/external support</done>
</task>

<task type="auto">
  <name>Task 3: Implement SToc component</name>
  <files>packages/core/src/components/SToc.astro</files>
  <action>
    Create `SToc.astro` that:
    - Imports `slidesData` from `virtual:slidastro/slides`.
    - Iterates over `slidesData.slides`.
    - Renders a list of links to slides that have a `title`.
    - Supports basic props like `columns` or `listStyle`.
  </action>
  <verify>
    <automated>test -f packages/core/src/components/SToc.astro</automated>
  </verify>
  <done>SToc component implemented using virtual slides data</done>
</task>

<task type="auto">
  <name>Task 4: Update renderer for component support</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    Update `renderer.ts` to ensure `<s-link>` and `<s-toc>` (and other future s- components) are preserved or transformed correctly in standard Markdown slides.
    If using Custom Elements for .md support, ensure they are rendered as valid custom tags.
  </action>
  <verify>
    <automated>grep "s-link" packages/core/src/renderer.ts || echo "Handled by default HTML support"</automated>
  </verify>
  <done>Rendering pipeline updated to support new content components</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Content -> Browser | SLink navigates user to internal/external URLs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | SLink | mitigate | Validate `to` prop to ensure it only links to valid slide numbers or predefined keywords. |
| T-14-02 | Information Disclosure | SToc | mitigate | Ensure SToc only displays titles of slides the user is authorized to see (though Slidastro is usually client-local). |
</threat_model>

<verification>
1. Verify SLink renders correctly in an Astro slide.
2. Verify SToc renders a list of links based on the presentation content.
</verification>

<success_criteria>
- SLink supports internal slide navigation.
- SToc generates a functional Table of Contents.
</success_criteria>

<output>
After completion, create `.planning/phases/14-content-built-in-components/14-01-SUMMARY.md`
</output>
