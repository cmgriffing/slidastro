---
phase: 13-core-click-logic-state
plan: 02
type: execute
wave: 2
depends_on: [13-01]
files_modified: [
  packages/core/src/components/SClicks.astro,
  packages/core/src/components/SSwitch.astro,
  packages/core/src/plugins/rehype-clicks.ts,
  packages/core/src/renderer.ts,
  packages/core/src/utils/clicks.ts
]
autonomous: true
requirements: [CLICK-02, CLICK-03]
---

<objective>
Implement advanced reveal components <s-clicks> and <s-switch> to enable sequential and conditional visibility logic.

Purpose: Allow users to group elements for sequential reveals or switch between content blocks based on click ranges.
Output: SClicks.astro and SSwitch.astro components, plus updated AST plugins to handle recursive indexing.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-click-logic-state/13-RESEARCH.md
@.planning/phases/13-core-click-logic-state/13-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Refine click range logic and implement components</name>
  <files>packages/core/src/utils/clicks.ts, packages/core/src/components/SClicks.astro, packages/core/src/components/SSwitch.astro</files>
  <action>
    1. Update `packages/core/src/utils/clicks.ts` to ensure `checkVisibility` supports single-click visibility (e.g. range "1" only matches click 1).
    2. Create `packages/core/src/components/SClicks.astro` which simply renders its slot.
    3. Create `packages/core/src/components/SSwitch.astro` which also renders its slot.
    - These components act as "markers" for the AST plugins to apply specific logic to their children.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test clicks.test.ts</automated>
  </verify>
  <done>Components created and range logic refined.</done>
</task>

<task type="auto">
  <name>Task 1: Update AST plugins for recursive child indexing</name>
  <files>packages/core/src/plugins/rehype-clicks.ts, packages/core/src/renderer.ts</files>
  <action>
    1. Update `packages/core/src/plugins/rehype-clicks.ts`:
       - When encountering an `s-clicks` (or `SClicks`) element:
         - Visit its children and automatically assign sequential `s-click` indices to each.
       - When encountering an `s-switch` (or `SSwitch`) element:
         - Visit its children and assign single-click indices (e.g. 1, 2, 3 instead of 1+, 2+, 3+).
    2. Apply similar logic to `packages/core/src/renderer.ts` for standard Markdown slides using the `ClickIndexer`.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test components-indexing.test.ts</automated>
  </verify>
  <done>SClicks and SSwitch components correctly manage their children's click indices during transformation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Component → Child AST | The parent component influences the properties of its children. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-03 | Denial of Service | rehype-clicks | mitigate | Limit recursion depth if necessary, though standard presentation trees are shallow. |
</threat_model>

<success_criteria>
1. Wrapping elements in `<s-clicks>` reveals them one-by-one.
2. Wrapping elements in `<s-switch>` shows only one at a time for each click index.
3. Nested clicks (e.g., `s-click` inside `s-clicks`) are handled correctly by the `ClickIndexer`.
</success_criteria>

<output>
After completion, create `.planning/phases/13-core-click-logic-state/13-02-SUMMARY.md`
</output>
