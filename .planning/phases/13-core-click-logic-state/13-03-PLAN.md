---
phase: 13-core-click-logic-state
plan: 03
type: execute
wave: 3
depends_on: [13-02]
files_modified: [
  packages/core/src/virtual.ts,
  packages/core/src/renderer.ts,
  packages/client/src/state.ts,
  packages/core/src/templates/SlideView.astro
]
autonomous: true
requirements: [CLICK-04, CLICK-05]
---

<objective>
Expose global variables $page and $total and ensure accurate click state synchronization across all components and islands.

Purpose: Provide standard presentation metadata to the user and ensure consistent state in multi-framework environments.
Output: Variable injection logic in renderer and virtual modules, and updated state initialization.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-click-logic-state/13-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: Implement $page and $total variable injection</name>
  <files>packages/core/src/renderer.ts, packages/core/src/virtual.ts</files>
  <action>
    1. In `packages/core/src/renderer.ts`, add a pre-processing step to `renderSlide` and `renderMarkdown` that replaces `{{ $page }}` and `{{ $total }}` (and variants like `$page.value`) with their actual values.
    2. In `packages/core/src/virtual.ts`, when generating the virtual module for a slide:
       - Inject `export const $page = ...` and `export const $total = ...` into the MDX/Astro scope if possible, or ensure they are available to the rendering context.
       - Note: For MDX, these can be added to the frontmatter or injected via a remark plugin.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test variable-injection.test.ts</automated>
  </verify>
  <done>$page and $total are accurately replaced in Markdown and available in component templates.</done>
</task>

<task type="auto">
  <name>Task 1: Accurate total clicks calculation and store sync</name>
  <files>packages/client/src/state.ts, packages/core/src/templates/SlideView.astro</files>
  <action>
    1. Ensure that the maximum click index discovered during AST transformation is passed back to the client.
    2. In `SlideView.astro`, ensure `$clicksTotal` is correctly initialized from the slide data on every navigation.
    3. Verify that NanoStore synchronization (via BroadcastChannel) correctly handles click updates between the main window and any islands (React, Vue, etc.).
  </action>
  <verify>
    <automated>cypress run --spec "cypress/e2e/clicks.cy.ts"</automated>
  </verify>
  <done>Total clicks are accurately calculated and synchronized across all framework islands.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| State Store → Island | NanoStore state flows into framework-specific islands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Information Disclosure | state.ts | accept | Slide state is not sensitive. |
</threat_model>

<success_criteria>
1. Global variables `$page` and `$total` display correctly in slides.
2. `next()` and `prev()` actions correctly respect the `totalClicks` for each slide.
3. Multiple islands on the same slide synchronize their click visibility state instantly.
</success_criteria>

<output>
After completion, create `.planning/phases/13-core-click-logic-state/13-03-SUMMARY.md`
</output>
