---
phase: 13-core-click-logic-state
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  packages/core/package.json,
  packages/core/src/utils/indexing.ts,
  packages/core/src/plugins/rehype-clicks.ts,
  packages/core/src/renderer.ts,
  packages/core/src/index.ts,
  packages/core/test/indexing.test.ts
]
autonomous: true
requirements: [CLICK-01, CLICK-05]
---

<objective>
Establish the core click indexing logic and AST transformation plugins to support s-click and s-after directives.

Purpose: Provide a unified way to assign click indices across standard Markdown and MDX/Astro slides.
Output: ClickIndexer utility, rehype-clicks plugin, and updated markdown-it rendering logic.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-core-click-logic-state/13-RESEARCH.md
@packages/core/src/renderer.ts
@packages/core/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 0: Add dependencies and ClickIndexer utility</name>
  <files>packages/core/package.json, packages/core/src/utils/indexing.ts, packages/core/test/indexing.test.ts</files>
  <action>
    1. Add `unist-util-visit` to `packages/core/package.json`.
    2. Create `packages/core/src/utils/indexing.ts` with a `ClickIndexer` class that handles absolute and relative indexing.
    - `resolve(directive: string, value?: string): number`
    - Absolute: `s-click="3"` -> returns 3, sets `lastResolvedIndex = 3`.
    - Relative Default: `s-click` -> returns `++lastResolvedIndex`.
    - Relative After: `s-after` -> returns `lastResolvedIndex`.
    3. Create `packages/core/test/indexing.test.ts` to verify the logic.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test indexing.test.ts</automated>
  </verify>
  <done>ClickIndexer implemented and tested with absolute and relative (including s-after) indexing.</done>
</task>

<task type="auto">
  <name>Task 1: Implement rehype-clicks plugin</name>
  <files>packages/core/src/plugins/rehype-clicks.ts, packages/core/src/index.ts</files>
  <action>
    1. Create `packages/core/src/plugins/rehype-clicks.ts`.
    2. Implement a rehype plugin that visits all elements:
    - If `sClick` or `sAfter` properties exist:
      - Assign a `data-step-click` attribute using `ClickIndexer`.
      - Add `slidastro-click` and `slidastro-click-hidden` to `className`.
    3. Register `rehypeClicks` in `packages/core/src/index.ts` within the `mdx()` integration configuration.
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test rehype-clicks.test.ts</automated>
  </verify>
  <done>MDX slides correctly transform s-click and s-after into data-step-click attributes.</done>
</task>

<task type="auto">
  <name>Task 2: Update markdown-it rendering logic</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    1. Update `renderSlide` in `packages/core/src/renderer.ts` to replace the old `step-click` logic with the new `s-click` and `s-after` support.
    2. Use `ClickIndexer` instance per slide to maintain state during replacement.
    3. Ensure it supports both tag attributes (e.g., `<div s-click>`) and custom tags if used in Markdown slides.
    4. Note: Use `s-` prefix per project conventions (per D-13-01).
  </action>
  <verify>
    <automated>pnpm --filter @slidastro/core test renderer.test.ts</automated>
  </verify>
  <done>Standard Markdown slides support s-click and s-after directives using the unified ClickIndexer logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Parser → AST | Untrusted Markdown content is converted to AST for transformation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Tampering | rehype-clicks | mitigate | Ensure AST transformation only modifies properties and classes, not injecting scripts. |
| T-13-02 | Info Disclosure | indexing.ts | accept | Indexing logic is public and doesn't handle sensitive data. |
</threat_model>

<success_criteria>
1. ClickIndexer correctly handles absolute and relative indexing.
2. `s-after` returns the same index as the previous `s-click` in the same sequence.
3. Both MDX and standard MD slides produce `data-step-click` attributes and `slidastro-click` classes.
</success_criteria>

<output>
After completion, create `.planning/phases/13-core-click-logic-state/13-01-SUMMARY.md`
</output>
