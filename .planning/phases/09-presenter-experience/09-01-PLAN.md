---
phase: 09-presenter-experience
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [packages/cli/src/index.ts]
autonomous: true
requirements: [PRES-06]

must_haves:
  truths:
    - "Running 'slidastro dev' logs the Main URL"
    - "Running 'slidastro dev' logs the Presenter URL"
    - "Running 'slidastro dev' logs the Overview URL"
    - "Running 'slidastro dev' logs the Print URL"
  artifacts:
    - path: "packages/cli/src/index.ts"
      provides: "URL discovery in CLI"
  key_links:
    - from: "packages/cli/src/index.ts"
      to: "Astro dev server address"
      via: "await dev()"
---

<objective>
Update the CLI to automatically discover and log all relevant deep links (Main, Presenter, Overview, and Print) when starting the dev server.

Purpose: Provide immediate access to all slide viewing modes for a better developer experience.
Output: Refined CLI logging in `slidastro dev`.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-presenter-experience/RESEARCH.md
@packages/cli/src/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLI to log discovered URLs</name>
  <files>packages/cli/src/index.ts</files>
  <action>
    - Capture the return value of `dev()` (an Astro `DevServer` object).
    - Extract the base URL from `server.address` (in Astro 5, this is a URL object).
    - Format and log the following URLs:
      - Main: `base`
      - Presenter: `${base}presenter/1`
      - Overview: `${base}overview`
      - Print: `${base}print`
    - Use clear and consistent styling for the logs (colors/icons via standard console methods).
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/cli exec slidastro dev demo.md --port 4321 --help | grep -q "slidastro dev"</automated>
  </verify>
  <done>
    - Running `slidastro dev` displays all four URLs in the console.
    - URLs correctly resolve to the intended routes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI -> Process | Logging URLs is a local operation, no external trust boundary crossed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | packages/cli/src/index.ts | accept | Logging local URLs to console is standard dev server behavior. |
</threat_model>

<verification>
Start the dev server using a sample presentation (e.g. `pnpm dev tests/slides.md`) and verify the console output.
</verification>

<success_criteria>
- The dev server starts correctly.
- The console output shows deep links for Main, Presenter, Overview, and Print modes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-presenter-experience/09-01-SUMMARY.md`
</output>
