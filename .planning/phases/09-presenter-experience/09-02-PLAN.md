---
phase: 09-presenter-experience
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/templates/PresenterView.astro
  - packages/core/src/components/PresenterTimer.astro
  - packages/core/src/components/PresenterClock.astro
autonomous: true
requirements: [PRES-07]

must_haves:
  truths:
    - "Presenter Mode has a top bar with Timer and Clock"
    - "Presenter Mode shows 'Next Slide' preview in the sidebar"
    - "Notes are visible and correctly formatted in the sidebar"
  artifacts:
    - path: "packages/core/src/templates/PresenterView.astro"
      provides: "New Slidev-style console grid"
    - path: "packages/core/src/components/PresenterTimer.astro"
      provides: "Elapsed time displays (Total/Slide)"
    - path: "packages/core/src/components/PresenterClock.astro"
      provides: "Real-time clock display"
  key_links:
    - from: "packages/core/src/templates/PresenterView.astro"
      to: "packages/core/src/components/SlidePreview.astro"
      via: "Astro component usage"
---

<objective>
Refactor the Presenter Mode UI to match Slidev's professional console layout, improving usability and feedback for the speaker.

Purpose: Achieve presenter experience parity with Slidev.
Output: Refactored `PresenterView.astro` and new components.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-presenter-experience/RESEARCH.md
@packages/core/src/templates/PresenterView.astro
@packages/core/src/components/SlidePreview.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Presenter components (Timer & Clock)</name>
  <files>packages/core/src/components/PresenterTimer.astro, packages/core/src/components/PresenterClock.astro</files>
  <action>
    - **PresenterTimer.astro**: Implement a display component for total and per-slide elapsed time.
      - Refactor the existing timer logic from `PresenterView.astro` into a clean, reusable component.
      - Use `@slidastro/client`'s `$timer` store to update the display.
      - Style it for professional presentation (monospace fonts, subtle coloring).
    - **PresenterClock.astro**: Implement a simple real-time clock component using `setInterval`.
  </action>
  <verify>
    <automated>ls packages/core/src/components/PresenterTimer.astro packages/core/src/components/PresenterClock.astro</automated>
  </verify>
  <done>
    - Both components are created and functional.
    - Timer correctly displays total and per-slide elapsed time.
    - Clock correctly displays current time.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactor PresenterView.astro to Slidev-style Console</name>
  <files>packages/core/src/templates/PresenterView.astro</files>
  <action>
    - Refactor the existing grid to use a 3-section layout:
      - **Top Bar**: Status bar containing `PresenterTimer`, `PresenterClock`, slide number (X/Y), and clicks/total-clicks indicator.
      - **Main Area**: Large current slide preview using `SlidePreview.astro` (with a larger scale, e.g., 0.6).
      - **Sidebar**: Split vertically into:
        - **Sidebar Top**: "Next Slide" preview using `SlidePreview.astro` (with a smaller scale, e.g., 0.35).
        - **Sidebar Bottom**: Speaker notes with scrollable container and refined typography.
    - Update styling to match a professional, dark console aesthetic (Slidev-parity).
    - Ensure all components use UnoCSS for refinements where applicable.
  </action>
  <verify>
    <automated>grep -q "PresenterTimer" packages/core/src/templates/PresenterView.astro</automated>
  </verify>
  <done>
    - `PresenterView.astro` has the new console grid structure.
    - Top bar displays all required information.
    - Next slide preview is rendered correctly.
    - Sidebar correctly shows formatted notes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser -> Component | All components run in the browser's local context, using standard nanostores for state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Denial of Service | SlidePreview.astro | mitigate | Ensure SlidePreview renders static HTML/Content without the Presenter UI shell to avoid recursion (as identified in RESEARCH.md). |
</threat_model>

<verification>
Start the dev server, visit `/presenter/1`, and verify the new layout and component functionality.
</verification>

<success_criteria>
- Presenter mode UI is visually consistent with Slidev console.
- Timer, clock, and slide info are all functional in the top bar.
- Next slide preview correctly updates as you navigate.
</success_criteria>

<output>
After completion, create `.planning/phases/09-presenter-experience/09-02-SUMMARY.md`
</output>
