---
phase: 09-presenter-experience
plan: 03
type: execute
wave: 2
depends_on: [09-02]
files_modified:
  - packages/client/src/sync.ts
  - packages/client/src/state.ts
  - packages/core/src/components/PresenterNavigation.client.ts
autonomous: true
requirements: [PRES-08]

must_haves:
  truths:
    - "Navigation in one window instantly updates all other synced windows"
    - "Timer remains synchronized across all windows"
    - "Drawings sync correctly across windows"
  artifacts:
    - path: "packages/client/src/sync.ts"
      provides: "Robust state synchronization"
    - path: "packages/client/src/state.ts"
      provides: "Refined state and timer logic"
  key_links:
    - from: "Main window"
      to: "Presenter window"
      via: "BroadcastChannel / Vite HMR"
---

<objective>
Refine state synchronization to ensure a seamless experience across all slide viewing modes, preventing drift and loops.

Purpose: Achieve professional-grade synchronization robustness.
Output: Refined sync logic and state management.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-presenter-experience/RESEARCH.md
@packages/client/src/sync.ts
@packages/client/src/state.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Improve Sync and Timer State Handling</name>
  <files>packages/client/src/sync.ts, packages/client/src/state.ts</files>
  <action>
    - Refine timer synchronization:
      - When syncing a "running" timer, include the `lastStarted` timestamp to ensure all windows can calculate the same elapsed time relative to their own clocks.
      - Implement a mechanism to periodically resync the "elapsed" time to prevent long-term drift between windows.
    - Review and harden the `isReceiving` flags in `sync.ts` to prevent recursive feedback loops across all synced stores (page, clicks, timer, drawings).
    - Ensure `resetSlideTimer` correctly resets the per-slide elapsed time and syncs this reset across windows.
  </action>
  <verify>
    <automated>grep -q "lastStarted" packages/client/src/sync.ts</automated>
  </verify>
  <done>
    - Timer state is robustly synchronized.
    - Feedback loops are prevented.
    - Per-slide timer resets are correctly handled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Final Navigation Polish & Verification</name>
  <files>packages/core/src/components/PresenterNavigation.client.ts, packages/core/src/templates/PresenterView.astro</files>
  <action>
    - Ensure `PresenterNavigation.client.ts` correctly handles the new UI structure (button selectors, keyboard shortcuts).
    - Verify that navigating from the Main window (standard slide view) correctly triggers a sync to the Presenter window, and vice-versa.
    - Test the synchronization in "Remote" mode (simulated by Vite HMR channel if using multiple browsers).
  </action>
  <verify>
    <automated>grep -q "initSync" packages/core/src/components/PresenterNavigation.client.ts</automated>
  </verify>
  <done>
    - All navigation events are correctly synced.
    - The experience is seamless and professional.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tab A -> BroadcastChannel -> Tab B | Same-origin sync is inherently trusted. |
| Vite HMR (Websocket) | Dev-only channel, used for cross-network sync. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | Information Disclosure | packages/client/src/sync.ts | accept | Sync data (page number, timer) is not sensitive information. |
</threat_model>

<verification>
Start the dev server, open multiple tabs (Main, Presenter, Overview).
- Change slide in Main: Presenter and Overview should update.
- Start/Pause/Reset timer in Presenter: All windows should reflect the change.
- Perform a drawing in Main (if drawing tool is open): Presenter should show the drawing.
</verification>

<success_criteria>
- Instantaneous sync between all windows (< 50ms latency in same browser).
- Timer displays match perfectly across windows (zero visible drift).
- Navigation is reliable and follows user intent across windows.
</success_criteria>

<output>
After completion, create `.planning/phases/09-presenter-experience/09-03-SUMMARY.md`
</output>
