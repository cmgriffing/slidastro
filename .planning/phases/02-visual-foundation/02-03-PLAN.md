---
phase: 02-visual-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/types/src/index.ts
  - packages/core/src/virtual.ts
  - packages/core/src/renderer.ts
  - packages/core/src/templates/SlideView.astro
  - packages/core/src/layouts/Default.astro
  - packages/core/src/layouts/Cover.astro
  - packages/core/src/layouts/TwoCols.astro
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Slides use custom layouts from the local 'layouts/' directory (relative to slides.md)"
    - "The layout resolver prioritizes local layouts over built-in ones"
    - "Slots (::right::) work consistently across all layout types"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "Updated SlidastroData interface"
    - path: "packages/core/src/layouts/Default.astro"
      provides: "Base built-in layout"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "local layout mapping"
      via: "scanning user directory"
---

<objective>
Implement a flexible layout system that supports both built-in defaults and user-provided custom layouts, and update types to include layout mapping.

Purpose: Empowers users to create unique presentation structures while providing a solid baseline.
Output: Dynamic layout resolver with support for local and built-in components.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SlidastroData Type</name>
  <files>packages/types/src/index.ts</files>
  <action>
    Add `layoutsMap: Record<string, string>` to the `SlidastroData` interface in `packages/types/src/index.ts`.
  </action>
  <verify>
    <automated>grep "layoutsMap" packages/types/src/index.ts</automated>
  </verify>
  <done>SlidastroData interface includes layoutsMap</done>
</task>

<task type="auto">
  <name>Task 2: Discover Local Layouts in Virtual Module</name>
  <files>packages/core/src/virtual.ts</files>
  <action>
    Update `slidastroVitePlugin` to:
    1. Resolve the `layouts/` directory relative to the `entry` file.
    2. Use `glob` (from `fast-glob` or similar) to scan for `.astro` files in that directory.
    3. Construct a mapping of layout names (filename without extension) to their absolute paths.
    4. Pass this mapping to the virtual module.
  </action>
  <verify>
    <automated>grep "glob" packages/core/src/virtual.ts</automated>
  </verify>
  <done>Local layout discovery is implemented</done>
</task>

<task type="auto">
  <name>Task 3: Implement Built-in Layouts and Slot Logic</name>
  <files>packages/core/src/renderer.ts, packages/core/src/layouts/Default.astro, packages/core/src/layouts/TwoCols.astro</files>
  <action>
    1. Update `packages/core/src/renderer.ts` with a function to extract named slots (e.g., `::right::`) from markdown content.
    2. Create `Default.astro`, `Cover.astro`, and `TwoCols.astro` in `packages/core/src/layouts/`.
    3. Use standard Astro slots (`<slot />`, `<slot name="right" />`) in layouts.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/Default.astro</automated>
  </verify>
  <done>Built-in layouts support named slots</done>
</task>

<task type="auto">
  <name>Task 4: Implement Dynamic Layout Resolver</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    1. Import the built-in layouts.
    2. Resolve the requested layout from `slide.frontmatter.layout`.
    3. Check the `layoutsMap` from the virtual module for local matches.
    4. If no local match, fall back to built-ins or the 'default' layout.
    5. Dynamically render the layout component using `Astro.glob` or passing the component itself if possible.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Layouts are resolved with priority: User > Built-in</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| user path -> layout import | Potential for arbitrary code execution via .astro file |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | layouts/ | accept | Slidastro is a developer tool; it is expected to execute local user code during development. |
</threat_model>

<verification>
1. Create a `layouts/Custom.astro` next to `slides.md`.
2. Set `layout: Custom` on a slide and verify it uses the local component.
3. Verify that `layout: two-cols` still works (falling back to built-in).
</verification>

<success_criteria>
1. User layouts are discovered and used correctly.
2. Built-in layouts provide a reliable fallback.
3. Content splitting into slots remains functional.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-03-SUMMARY.md`
</output>
