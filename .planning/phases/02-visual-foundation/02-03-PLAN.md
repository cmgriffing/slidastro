---
phase: 02-visual-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/types/src/index.ts
  - packages/core/src/renderer.ts
  - packages/core/src/virtual.ts
  - packages/core/src/layouts/Default.astro
  - packages/core/src/layouts/Cover.astro
  - packages/core/src/layouts/TwoCols.astro
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Slides use the layout specified in their frontmatter"
    - "The 'two-cols' layout correctly splits content using ::right::"
  artifacts:
    - path: "packages/core/src/layouts/Default.astro"
      provides: "Base slide layout"
    - path: "packages/core/src/layouts/TwoCols.astro"
      provides: "Split-screen layout"
  key_links:
    - from: "packages/core/src/templates/SlideView.astro"
      to: "packages/core/src/layouts/Default.astro"
      via: "dynamic layout resolution"
---

<objective>
Implement the layout system with support for named slots and built-in components like 'cover' and 'two-cols'.

Purpose: Provides structure to slide content.
Output: A set of built-in Astro layout components and a resolver.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/renderer.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Slot Support to Types and Renderer</name>
  <files>packages/types/src/index.ts, packages/core/src/renderer.ts, packages/core/src/virtual.ts</files>
  <action>
    1. Update `SlideInfoBase` in `packages/types/src/index.ts` to include `contentHTML?: string` and `slots?: Record<string, string>`.
    2. In `packages/core/src/renderer.ts`, implement a function to split slide content by `::slotname::` markers (similar to Slidev).
    3. Update `packages/core/src/virtual.ts` to render each slot separately and store them in the `slots` record.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/types build && pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Slots are supported in the data pipeline</done>
</task>

<task type="auto">
  <name>Task 2: Implement Built-in Layout Components</name>
  <files>packages/core/src/layouts/Default.astro, packages/core/src/layouts/Cover.astro, packages/core/src/layouts/TwoCols.astro</files>
  <action>
    Create the following Astro components in `packages/core/src/layouts/`:
    1. `Default.astro`: Standard layout with a single `<slot />`.
    2. `Cover.astro`: Centered layout for title slides.
    3. `TwoCols.astro`: Two-column layout using named slots (`<slot name="default" />` and `<slot name="right" />`).
    Use Tailwind or basic CSS for layout styling (e.g., flexbox/grid).
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/Default.astro packages/core/src/layouts/Cover.astro packages/core/src/layouts/TwoCols.astro</automated>
  </verify>
  <done>Built-in layout components exist</done>
</task>

<task type="auto">
  <name>Task 3: Implement Layout Resolver and Update SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    1. Determine the layout name from `slide.frontmatter.layout` (defaulting to 'default').
    2. Dynamically import or map the layout name to its Astro component.
    3. Render the selected layout component, passing the slots to it. 
       Note: For named slots, use `<Fragment slot="right" set:html={slide.slots.right} />`.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>SlideView renders slides using the correct layout</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| frontmatter.layout -> dynamic import | Untrusted layout name from markdown selects component |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | layout name | mitigate | Use a whitelist or map for built-in layouts to avoid arbitrary file imports. |
</threat_model>

<verification>
1. Create a slide with `layout: cover` and verify it's centered.
2. Create a slide with `layout: two-cols` and content separated by `::right::`, and verify it shows two columns.
3. Verify that a slide with no layout uses the 'default' layout.
</verification>

<success_criteria>
1. Built-in layouts are functional.
2. Content splitting into slots works as expected.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-03-SUMMARY.md`
</output>
