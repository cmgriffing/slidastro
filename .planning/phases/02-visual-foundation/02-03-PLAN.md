---
phase: 02-visual-foundation
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - packages/types/src/index.ts
  - packages/core/src/virtual.ts
  - packages/core/src/renderer.ts
  - packages/core/src/layouts/Default.astro
  - packages/core/src/layouts/Cover.astro
  - packages/core/src/layouts/TwoCols.astro
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Slides use custom layouts from the local 'layouts/' directory (relative to slides.md)"
    - "Layout resolver correctly falls back to built-ins if local layout is missing"
    - "Named slots (e.g., ::right::) are correctly parsed and mapped to layout slots"
  artifacts:
    - path: "packages/core/src/layouts/Default.astro"
      provides: "Base built-in layout with default slot"
    - path: "packages/core/src/layouts/TwoCols.astro"
      provides: "Two-column layout with named slots"
    - path: "packages/core/src/virtual.ts"
      provides: "Local layoutsMap for slide rendering"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "local layout files"
      via: "scanning user directory with glob"
---

<objective>
Implement a flexible layout system that supports both built-in defaults and user-provided custom layouts. Support Slidev-compatible slot mapping (::name::) to Astro slots.

Purpose: Empowers users to customize presentation structure while maintaining Slidev compatibility.
Output: Dynamic layout resolver with support for local layouts and named slots.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discover Local Layouts in Virtual Module</name>
  <files>packages/core/src/virtual.ts, packages/types/src/index.ts</files>
  <action>
    1. Add `layoutsMap: Record<string, string>` to `SlidastroData` in `packages/types/src/index.ts`.
    2. Update `slidastroVitePlugin` to scan for `.astro` files in a `layouts/` directory relative to the `entry` file.
    3. Construct a mapping of layout names to their absolute paths.
    4. Pass this mapping to the virtual module.
  </action>
  <verify>
    <automated>grep "layoutsMap" packages/core/src/virtual.ts</automated>
  </verify>
  <done>Local layouts are discovered and mapped in the virtual module.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Slot Mapping in Renderer</name>
  <files>packages/core/src/renderer.ts, packages/types/src/index.ts</files>
  <behavior>
    - Content without slot markers goes to the `default` slot.
    - `::right::` content goes to the `right` slot.
    - Slot markers themselves are removed from the output.
  </behavior>
  <action>
    1. Update `SlideInfo` to include `slots?: Record<string, string>`.
    2. Update `renderer.ts` to split markdown content into named slots based on `::slot-name::` markers.
    3. Render each slot's content independently to HTML and store in the `slots` record.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Markdown content is split into named slots and rendered to HTML.</done>
</task>

<task type="auto">
  <name>Task 3: Create Built-in Layouts</name>
  <files>packages/core/src/layouts/Default.astro, packages/core/src/layouts/Cover.astro, packages/core/src/layouts/TwoCols.astro</files>
  <action>
    Create the three primary built-in layouts:
    - `Default.astro`: centered content with a default `<slot />`.
    - `Cover.astro`: large title and subtitle slots.
    - `TwoCols.astro`: two equal columns using `<slot />` and `<slot name="right" />`.
    Ensure each layout accepts `frontmatter` as a prop.
  </action>
  <verify>
    <automated>ls packages/core/src/layouts/TwoCols.astro</automated>
  </verify>
  <done>Built-in layouts with slot support are created.</done>
</task>

<task type="auto">
  <name>Task 4: Dynamic Layout Resolver in SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    1. Resolve the layout name from `slide.frontmatter.layout` (fallback to 'default').
    2. Select the component:
       - Check `layoutsMap` for a local match.
       - Use a built-in mapping for core layouts.
    3. Dynamically import and render the layout component.
    4. Map `slide.slots` to the layout's named slots using Astro's `set:html`.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>SlideView dynamically resolves and renders layouts with full slot support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| layout name -> path resolution | Potential for arbitrary path injection if not restricted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Tampering | layouts/ | accept | Slidastro is a dev tool; execution of local user code is intended behavior. |
</threat_model>

<verification>
1. Set `layout: two-cols` on a slide and verify that `::right::` content appears in the second column.
2. Create a `layouts/Custom.astro` and verify that `layout: Custom` works correctly.
3. Verify that frontmatter properties are passed to and accessible within layouts.
</verification>

<success_criteria>
1. Layout resolution works with priority: User > Built-in.
2. Slot mapping (`::right::`) is functional.
3. Default layout provides a reliable fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-03-SUMMARY.md`
</output>
