---
phase: 02-visual-foundation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - packages/core/src/components/SlideContainer.astro
  - packages/core/src/components/SlideScale.client.ts
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Slides maintain a consistent 16:9 aspect ratio regardless of window size"
    - "Slides are centered and scaled via CSS transforms"
  artifacts:
    - path: "packages/core/src/components/SlideContainer.astro"
      provides: "Visual wrapper for slides"
    - path: "packages/core/src/components/SlideScale.client.ts"
      provides: "Client-side scaling logic"
  key_links:
    - from: "packages/core/src/templates/SlideView.astro"
      to: "packages/core/src/components/SlideContainer.astro"
      via: "wrapping the slide content"
---

<objective>
Implement a responsive scaling system that maintains a fixed 16:9 aspect ratio (default) using CSS transforms.

Purpose: Ensures the presentation looks consistent on all screens.
Output: A SlideContainer component with client-side scaling logic.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SlideContainer Component</name>
  <files>packages/core/src/components/SlideContainer.astro</files>
  <action>
    Create a `SlideContainer.astro` component that defines the fixed dimensions for a slide (e.g., 980x551 for 16:9).
    The container should have `overflow: hidden` and `position: relative`.
    It should contain a `.slide-content` wrapper that will be scaled.
  </action>
  <verify>
    <automated>ls packages/core/src/components/SlideContainer.astro</automated>
  </verify>
  <done>SlideContainer component exists</done>
</task>

<task type="auto">
  <name>Task 2: Implement Scaling Logic</name>
  <files>packages/core/src/components/SlideScale.client.ts</files>
  <action>
    Implement a client-side script that:
    1. Calculates the scale factor: `min(window.innerWidth / slideWidth, window.innerHeight / slideHeight)`.
    2. Applies `transform: scale(scaleFactor)` to the `.slide-content` element.
    3. Listens to `resize` events to update the scale factor.
    Use `requestAnimationFrame` for smooth updates.
  </action>
  <verify>
    <automated>ls packages/core/src/components/SlideScale.client.ts</automated>
  </verify>
  <done>Scaling script is implemented</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Container into SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    1. Import `SlideContainer.astro`.
    2. Import the `SlideScale.client.ts` script (use `<script src="...">`).
    3. Wrap the slide rendering logic in the `SlideContainer`.
    4. Remove the hardcoded dimensions from `SlideView.astro`'s internal styles and rely on the container.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>SlideView uses the scaling container</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| window resize -> CSS | Dynamic scaling based on untrusted window dimensions |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Denial of Service | SlideScale.client.ts | mitigate | Ensure resize listener is throttled or uses requestAnimationFrame to avoid CPU spikes. |
</threat_model>

<verification>
1. Resize the browser window and verify that the slide content scales proportionally.
2. Verify that the slide stays centered in the viewport.
3. Check for any "jitter" or layout shift during resizing.
</verification>

<success_criteria>
1. Slides maintain their aspect ratio on any screen size.
2. Content is never cut off or stretched incorrectly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-02-SUMMARY.md`
</output>
