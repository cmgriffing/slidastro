---
phase: 02-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/types/src/index.ts
  - packages/core/package.json
  - packages/core/src/renderer.ts
  - packages/core/src/virtual.ts
  - packages/core/src/index.ts
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-03, VIS-04]
must_haves:
  truths:
    - "Markdown content is rendered as HTML with Shiki highlighting and KaTeX math"
    - "Utility CSS (UnoCSS/Tailwind) is available for use in slides and layouts"
    - "HMR updates slide content without full-page reloads for content changes"
  artifacts:
    - path: "packages/core/src/renderer.ts"
      provides: "Markdown rendering pipeline (markdown-it + Shiki + KaTeX)"
    - path: "packages/core/src/virtual.ts"
      provides: "Pre-rendered HTML in virtual module with HMR diffing"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "packages/core/src/renderer.ts"
      via: "renderMarkdown call"
---

<objective>
Implement the markdown rendering pipeline with Shiki syntax highlighting, KaTeX math support, and UnoCSS utility classes. Establish a surgical HMR mechanism that updates slide content without full-page reloads.

Purpose: Provides the core visual experience and high-speed developer feedback loop.
Output: Rendered HTML with styling and functional surgical HMR.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Types and Install Dependencies</name>
  <files>packages/types/src/index.ts, packages/core/package.json</files>
  <action>
    1. Add `contentHTML?: string` to `SlideInfo` in `packages/types/src/index.ts`.
    2. Install dependencies in `packages/core`: `markdown-it`, `@shikijs/markdown-it`, `markdown-it-katex`, `katex`, and `@unocss/astro`.
    3. Register UnoCSS integration in `packages/core/src/index.ts` within the `astro:config:setup` hook.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build && grep "unocss" packages/core/package.json</automated>
  </verify>
  <done>Types updated and rendering/styling dependencies installed and registered.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Markdown Renderer</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    Create `renderer.ts` that exports `renderMarkdown(content: string)`.
    - Configure `markdown-it` with `html: true`, `breaks: true`, and `linkify: true`.
    - Plug in `@shikijs/markdown-it` with theme `vitesse-dark` (or similar).
    - Plug in `markdown-it-katex`.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Markdown renderer is functional with Shiki and KaTeX.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Surgical HMR in Virtual Module</name>
  <files>packages/core/src/virtual.ts</files>
  <behavior>
    - When a slide's content changes, only its `contentHTML` should update.
    - If the number of slides or global metadata changes, the entire virtual module is invalidated.
    - Use a module-level cache to store previous slide data for diffing.
  </behavior>
  <action>
    Update `slidastroVitePlugin` in `virtual.ts`:
    1. Use `renderMarkdown` to populate `contentHTML` for each slide.
    2. In `handleHotUpdate`, re-parse the entry file and compare with the cached state.
    3. If only slide content changed (no metadata/count changes), send a custom HMR message `slidastro:update-content` with the new HTML.
    4. Otherwise, invalidate the whole module as before.
  </action>
  <verify>
    <automated>grep "update-content" packages/core/src/virtual.ts</automated>
  </verify>
  <done>Surgical HMR logic is implemented in the Vite plugin.</done>
</task>

<task type="auto">
  <name>Task 4: Update SlideView Template</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    1. Import KaTeX CSS in the `<head>`.
    2. Update the slide content area to use `set:html={slide.contentHTML}`.
    3. Add a client-side `<script>` to listen for `slidastro:update-content` and update the DOM element without a reload if the current slide matches.
  </action>
  <verify>
    <automated>grep "set:html" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>SlideView renders rich HTML and handles client-side surgical updates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| markdown -> HTML | Content from slides.md rendered as HTML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | slides.md | accept | Local file access only. |
| T-02-02 | Information Disclosure | renderer | mitigate | Ensure Shiki/KaTeX errors don't expose server paths. |
</threat_model>

<verification>
1. Verify Shiki classes are present in rendered code blocks.
2. Verify KaTeX renders formulas correctly.
3. Verify that editing slide text triggers an HMR update in the browser without a full page reload (spinner doesn't appear).
</verification>

<success_criteria>
1. Markdown renders to rich HTML with syntax highlighting and math.
2. UnoCSS is functional (test with a utility class in SlideView).
3. Surgical HMR works for content-only changes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-01-SUMMARY.md`
</output>
