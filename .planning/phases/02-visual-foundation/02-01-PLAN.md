---
phase: 02-visual-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/package.json
  - packages/core/src/renderer.ts
  - packages/core/src/virtual.ts
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-03, VIS-04]
must_haves:
  truths:
    - "Slide content rendered as HTML instead of raw markdown"
    - "Code blocks show syntax highlighting"
    - "Math formulas render correctly with KaTeX"
  artifacts:
    - path: "packages/core/src/renderer.ts"
      provides: "Markdown rendering with Shiki and KaTeX"
    - path: "packages/core/src/virtual.ts"
      provides: "Pre-rendered HTML in virtual module"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "packages/core/src/renderer.ts"
      via: "import and call renderMarkdown"
---

<objective>
Implement the core markdown rendering pipeline with support for syntax highlighting (Shiki) and math formulas (KaTeX).

Purpose: Provides the visual foundation for content display.
Output: Pre-rendered HTML in slide data with rich formatting.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Markdown Rendering Dependencies</name>
  <files>packages/core/package.json</files>
  <action>
    Add `markdown-it`, `@shikijs/markdown-it`, `markdown-it-katex`, and `katex` to `packages/core`.
    Install them using `pnpm install` in the workspace.
  </action>
  <verify>
    <automated>grep -E "markdown-it|shikijs|katex" packages/core/package.json</automated>
  </verify>
  <done>Dependencies are installed and listed in core's package.json</done>
</task>

<task type="auto">
  <name>Task 2: Implement Markdown Renderer</name>
  <files>packages/core/src/renderer.ts</files>
  <action>
    Create a `renderer.ts` that exports a `renderMarkdown(content: string)` function.
    Configure `markdown-it` to use `@shikijs/markdown-it` for syntax highlighting and `markdown-it-katex` for math.
    Ensure Shiki is configured with a default theme (e.g., 'vitesse-dark').
    Use `html: true` in markdown-it config to support embedded HTML.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Renderer utility is implemented and builds correctly</done>
</task>

<task type="auto">
  <name>Task 3: Integrate Renderer into Virtual Module and Page</name>
  <files>packages/core/src/virtual.ts, packages/core/src/templates/SlideView.astro</files>
  <action>
    1. In `packages/core/src/virtual.ts`, import the new renderer and update the virtual module generator to pre-render slide content into a `contentHTML` field.
    2. Update `packages/types` if necessary (though SlideInfo already has optional fields, we might need contentHTML). Actually, use the existing `content` field or add `contentHTML`.
    3. Update `packages/core/src/templates/SlideView.astro` to include KaTeX CSS and render `slide.contentHTML` using `set:html`.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/cli dev slides.md --no-open (check output for renderer usage or manual check in browser later)</automated>
  </verify>
  <done>Slide content is rendered as HTML with styles</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| markdown→HTML | Untrusted markdown content from slides.md converted to HTML |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | slides.md | accept | Local file-system access only; same-origin as user's dev environment. |
| T-02-02 | Information Disclosure | renderer | mitigate | Ensure Shiki does not leak system paths in error messages. |
</threat_model>

<verification>
1. Check that code blocks have Shiki classes/styles.
2. Check that math formulas (e.g., $E=mc^2$) are rendered as KaTeX elements.
3. Check that regular markdown (headings, lists) renders as HTML tags.
</verification>

<success_criteria>
1. Markdown content in slides.md is rendered as rich HTML in the browser.
2. Syntax highlighting is active for code blocks.
3. KaTeX formulas are visible and formatted correctly.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-01-SUMMARY.md`
</output>
