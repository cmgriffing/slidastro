---
phase: 02-visual-foundation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - packages/types/src/index.ts
  - packages/core/src/virtual.ts
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "Themes from npm packages (e.g., slidastro-theme-*) can be applied via frontmatter"
    - "Theme-provided layouts are discoverable and usable in slides"
    - "Theme global styles are injected into the presentation"
  artifacts:
    - path: "packages/core/src/virtual.ts"
      provides: "External theme asset resolution (layouts, styles)"
    - path: "packages/core/src/templates/SlideView.astro"
      provides: "Dynamic theme CSS injection"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "node_modules"
      via: "require.resolve for theme discovery"
---

<objective>
Establish a robust theme system framework that supports external npm packages. Ensure Slidev-compatible layout and style structures are supported within the Astro-based rendering pipeline.

Purpose: Enables extensibility and community-driven design through reusable theme packages.
Output: Integrated theme loader supporting external layouts and global CSS.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Theme Package Discovery</name>
  <files>packages/core/src/virtual.ts, packages/types/src/index.ts</files>
  <action>
    1. Add `themeCSS?: string` to `SlidastroConfig` in `packages/types/src/index.ts`.
    2. Update `slidastroVitePlugin` to:
       - Detect the `theme` name from headmatter.
       - Use `require.resolve` (or similar) to find the theme package. Support prefixes: `slidastro-theme-*`, `@slidastro/theme-*`, and `@slidastro-theme/*`.
       - If found, scan the package for `layouts/` and add them to `layoutsMap`.
       - Identify the global CSS file (e.g., `styles/index.css` or the `style` field in `package.json`).
       - Pass the resolved CSS path to the virtual module.
  </action>
  <verify>
    <automated>grep "require.resolve" packages/core/src/virtual.ts</automated>
  </verify>
  <done>Theme packages are discovered and their assets (layouts/CSS) are mapped.</done>
</task>

<task type="auto">
  <name>Task 2: Inject Theme Styles into SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    - Receive the `themeCSS` path from the virtual module.
    - Dynamically import or link the CSS file if present.
    - Ensure default Slidastro styles are applied if no theme is specified.
  </action>
  <verify>
    <automated>grep "themeCSS" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>External theme styles are correctly injected into the rendering page.</done>
</task>

<task type="auto">
  <name>Task 3: Verify with Mock Theme</name>
  <files>packages/core/src/virtual.ts</files>
  <action>
    Create a mock theme package `slidastro-theme-mock` in the monorepo for testing.
    - Include a simple layout and a CSS file that changes the background color.
    - Update `virtual.ts` tests to verify that `theme: mock` correctly resolves layouts and styles from this package.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>The theme system is verified to work with external package structures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| theme name -> module resolution | Resolution of arbitrary packages from node_modules |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | theme loader | mitigate | Validate theme names to ensure they follow expected patterns (prefixes) before resolving. |
</threat_model>

<verification>
1. Mock a `slidastro-theme-test` package and verify its CSS is injected.
2. Verify that a layout from the theme can be used in a slide.
3. Check that invalid theme names (not matching prefixes) are ignored or handled gracefully.
</verification>

<success_criteria>
1. Themes can be loaded from npm packages.
2. Theme-provided layouts are usable via `layout: [name]`.
3. Theme CSS is applied globally to the slides.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-04-SUMMARY.md`
</output>
