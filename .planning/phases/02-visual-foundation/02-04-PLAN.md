---
phase: 02-visual-foundation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - packages/types/src/index.ts
  - packages/core/src/virtual.ts
  - packages/core/src/templates/SlideView.astro
  - packages/core/src/renderer.ts
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "Slides can apply themes from npm packages following Slidev-style structure (layouts/styles)"
    - "Styles from themes are automatically injected into the slide view"
    - "Layouts provided by themes are discoverable by the layout resolver"
  artifacts:
    - path: "packages/types/src/index.ts"
      provides: "Updated SlidastroConfig interface"
    - path: "packages/core/src/templates/SlideView.astro"
      provides: "Dynamic theme CSS injection"
  key_links:
    - from: "packages/core/src/virtual.ts"
      to: "theme discovery"
      via: "scanning node_modules"
---

<objective>
Establish the theme system framework that supports external packages with Slidev-compatible layouts and styles, using robust resolution and updated configuration types.

Purpose: Enables community-driven design customization and extensibility.
Output: Integrated theme loader supporting layouts and global CSS from external modules.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/types/src/index.ts
@packages/core/src/virtual.ts
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SlidastroConfig Type</name>
  <files>packages/types/src/index.ts</files>
  <action>
    Add `themeCSS?: string` to the `SlidastroConfig` interface in `packages/types/src/index.ts`.
  </action>
  <verify>
    <automated>grep "themeCSS" packages/types/src/index.ts</automated>
  </verify>
  <done>SlidastroConfig interface includes themeCSS</done>
</task>

<task type="auto">
  <name>Task 2: Implement Robust External Theme Loader</name>
  <files>packages/core/src/virtual.ts</files>
  <action>
    Update `slidastroVitePlugin` to:
    1. Look for a `theme` name in the slide deck headmatter.
    2. Try to resolve the theme package (e.g., `slidastro-theme-[name]` or `@slidastro/theme-[name]`) using robust node resolution (`require.resolve` or similar).
    3. If found, scan the package for `layouts/` and `styles/index.css`.
    4. Add theme layouts to the `layoutsMap` (merging with user and built-ins).
    5. Pass the theme's CSS entry point to the virtual module via the `themeCSS` field in config.
  </action>
  <verify>
    <automated>grep "require.resolve" packages/core/src/virtual.ts</automated>
  </verify>
  <done>External theme package assets are discovered using robust resolution</done>
</task>

<task type="auto">
  <name>Task 3: Inject Theme Styles into SlideView</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Update `SlideView.astro` to:
    1. Accept the theme CSS path from the virtual module.
    2. Use an Astro `<style>` tag or dynamic import to inject the theme's global styles.
    3. Ensure default styles act as a fallback if no theme is specified.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Theme styles are applied to the presentation</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| theme name -> module resolution | Arbitrary module resolution if not restricted |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | theme loader | mitigate | Validate theme names to ensure they follow the expected prefix patterns (`@slidastro/theme-*`, etc.). |
</threat_model>

<verification>
1. Mock a `slidastro-theme-test` package with a custom layout and CSS.
2. Specify `theme: test` in slides.md and verify the styles are applied.
3. Verify that layouts from the theme are usable via `layout: [theme-layout-name]`.
</verification>

<success_criteria>
1. Slidev-compatible theme structure is supported.
2. Themes can provide both layouts and styling.
3. System is ready for official and community themes.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-04-SUMMARY.md`
</output>
