---
phase: 02-visual-foundation
plan: 04
type: execute
wave: 4
depends_on: ["02-03"]
files_modified:
  - packages/core/src/index.ts
  - packages/core/src/templates/SlideView.astro
autonomous: true
requirements: [VIS-02]
must_haves:
  truths:
    - "Slides apply a default theme's CSS"
    - "Users can potentially switch themes (at least via static CSS injection)"
  artifacts:
    - path: "packages/core/src/index.ts"
      provides: "Basic theme path resolution"
  key_links:
    - from: "packages/core/src/templates/SlideView.astro"
      to: "theme CSS"
      via: "dynamic link/import"
---

<objective>
Establish a basic theme system that allows applying global styles to the slide deck.

Purpose: Enables visual customization and future theme support.
Output: Integrated theme styles in the slide rendering pipeline.
</objective>

<execution_context>
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/workflows/execute-plan.md
@/Volumes/T7/repos/slidastro/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/core/src/templates/SlideView.astro
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Default Theme Styles</name>
  <files>packages/core/src/templates/SlideView.astro</files>
  <action>
    Define a set of default CSS variables and styles in `SlideView.astro` (or a separate CSS file) that mimics a standard presentation theme.
    Include styles for:
    - Base fonts and colors (Vitesse-like palette).
    - Headings (h1, h2, h3).
    - Lists and blockquotes.
    - Code block aesthetics (padding, borders).
  </action>
  <verify>
    <automated>grep -E "variable|color|font" packages/core/src/templates/SlideView.astro</automated>
  </verify>
  <done>Default presentation styles are applied</done>
</task>

<task type="auto">
  <name>Task 2: Basic Theme Support logic</name>
  <files>packages/core/src/index.ts, packages/core/src/templates/SlideView.astro</files>
  <action>
    1. Update the integration to look for a `theme` property in the slide deck's headmatter.
    2. For now, implement a simple way to inject a theme's CSS if it's found in `node_modules`. 
    3. Allow users to specify a local CSS file in frontmatter to override theme styles.
  </action>
  <verify>
    <automated>pnpm -w --filter @slidastro/core build</automated>
  </verify>
  <done>Basic theme selection is wired up</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| theme name -> CSS path | Potential for path traversal if not validated |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | theme loader | mitigate | Validate theme names against a strict pattern or use a known directory. |
</threat_model>

<verification>
1. Check that slides have consistent typography and colors out of the box.
2. Verify that changing the global `theme` (e.g., if a local CSS is provided) changes the look.
</verification>

<success_criteria>
1. A default theme is active and visually pleasing.
2. The infrastructure for loading external themes is started.
</success_criteria>

<output>
After completion, create `.planning/phases/02-visual-foundation/02-04-SUMMARY.md`
</output>
